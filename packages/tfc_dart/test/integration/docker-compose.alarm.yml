services:
  timescaledb:
    image: timescale/timescaledb:latest-pg17
    container_name: test-db-alarm
    restart: unless-stopped
    environment:
      - POSTGRES_DB=testdb
      - POSTGRES_USER=testuser
      - POSTGRES_PASSWORD=testpass
    ports:
      - 5444:5432
