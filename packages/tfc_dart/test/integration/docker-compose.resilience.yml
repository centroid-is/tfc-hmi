services:
  timescaledb:
    image: timescale/timescaledb:latest-pg17
    container_name: test-db-resilience
    restart: "no"
    environment:
      - POSTGRES_DB=testdb
      - POSTGRES_USER=testuser
      - POSTGRES_PASSWORD=testpass
    ports:
      - 5442:5432
