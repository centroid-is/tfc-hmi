name: jbtm
description: JBTM device data acquisition for TFC
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.1

dependencies:
  rxdart: ^0.28.0
  logger: ^2.4.0
  open62541: ^1.5.7+2

dependency_overrides:
  # open62541 dev branch: session-recreate freeze fix (open62541_dart#91) +
  # orphaned-monitored-item fix (open62541_dart#92), not yet in a pub release.
  # Head of `dev` on open62541_dart, 2026-08-29. SHA-pinned like postgres below
  # (pubspec.lock is gitignored). Remove once these ship to pub.
  open62541:
    git:
      url: https://github.com/centroid-is/open62541_dart.git
      ref: 1c44a8fef8bc9b007cf03f24e035caeb8b8b6adc

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
