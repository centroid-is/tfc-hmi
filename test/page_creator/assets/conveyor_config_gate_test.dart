import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:tfc/page_creator/assets/conveyor.dart';
import 'package:tfc/page_creator/assets/conveyor_gate.dart';

// CHILD-06: Config dialog gate management

void main() {
  group('Conveyor config gate management (CHILD-06)', () {
    test('Add Gate button creates a new ConveyorGateConfig in gates list', () {
      // After Task 2: tapping "Add Gate" should add a ConveyorGateConfig
      // to widget.config.gates
      fail('Wave 0 stub -- implement in Task 2');
    });

    test('gate row displays variant, side, and position summary', () {
      // After Task 2: each gate row shows "pneumatic - left @ 50%"
      fail('Wave 0 stub -- implement in Task 2');
    });

    test('Delete button removes gate from list', () {
      // After Task 2: tapping delete icon should remove the gate
      fail('Wave 0 stub -- implement in Task 2');
    });
  });
}
