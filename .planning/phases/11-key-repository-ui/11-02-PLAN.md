---
phase: 11-key-repository-ui
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: []
autonomous: false
requirements: [UIKY-01, UIKY-02, UIKY-03, UIKY-04, UIKY-05, UIKY-06]

must_haves:
  truths:
    - "User can visually see and interact with the Modbus protocol option in the key editor"
    - "Data type auto-lock behavior is correct and visible to the user"
    - "Poll group dropdown correctly reflects the selected server's configuration"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete Modbus key repository UI.

Purpose: Confirm that the Modbus config section renders correctly, protocol switching is intuitive, data type auto-lock visually indicates its state, and poll group dropdown works as expected. Widget tests validate logic but cannot catch visual layout issues.

Output: Human-verified approval of the key repository Modbus UI.
</objective>

<execution_context>
@/Users/jonb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-key-repository-ui/11-CONTEXT.md
@.planning/phases/11-key-repository-ui/11-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of Modbus key repository UI</name>
  <files></files>
  <action>
Present the verification checklist to the user. No code changes -- this is a visual/interaction verification checkpoint.

What was built: Modbus protocol config section in the key repository UI, including three-way protocol switching (OPC UA/M2400/Modbus), server alias dropdown, register type dropdown, address field, data type auto-lock, and poll group dropdown.
  </action>
  <verify>User approves or describes issues</verify>
  <done>User confirms the Modbus key repository UI renders correctly and all interactions work as expected</done>
  <what-built>Modbus protocol config section in the key repository UI, including three-way protocol switching (OPC UA/M2400/Modbus), server alias dropdown, register type dropdown, address field, data type auto-lock, and poll group dropdown.</what-built>
  <how-to-verify>
    1. Run the app: `flutter run -d linux` (or your preferred device)
    2. Navigate to Key Repository page
    3. If no keys exist, add a new key
    4. Expand a key card -- verify you see the "Device Type" row with OPC UA, M2400 (if JBTM servers configured), and Modbus chips
    5. Tap "Modbus" chip -- verify:
       - "Modbus Key Configuration" header appears with network icon
       - Server alias dropdown shows configured Modbus servers
       - Register type dropdown shows: coil, discreteInput, holdingRegister, inputRegister
       - Address field accepts numeric input
       - Data type dropdown shows uint16 by default
       - Poll group dropdown shows server's poll groups with intervals
    6. Change register type to "coil" -- verify:
       - Data type changes to "bit" automatically
       - Data type dropdown shows "Data Type (auto)" label
       - Data type dropdown is greyed out / disabled
    7. Change register type back to "holdingRegister" -- verify:
       - Data type resets to "uint16"
       - Data type dropdown is re-enabled with all 9 options
    8. Switch between OPC UA and Modbus -- verify config fields change appropriately
    9. Check collapsed card subtitle shows compact Modbus info (e.g. "holdingRegister[0] uint16 @ plc_1")
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/interaction issues</resume-signal>
</task>

</tasks>

<verification>
Human visual verification of the complete Modbus key repository UI flow.
</verification>

<success_criteria>
- User confirms all Modbus config fields render correctly
- Data type auto-lock visually indicates disabled state
- Protocol switching between all three protocols is smooth
- Layout matches existing M2400 section pattern
</success_criteria>

<output>
After completion, create `.planning/phases/11-key-repository-ui/11-02-SUMMARY.md`
</output>
