---
phase: 01-standalone-diverter-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/page_creator/assets/conveyor_gate.dart
autonomous: true
requirements:
  - SOLO-04
  - PAINT-03
  - VIS-01
  - VIS-02
  - PAINT-06

must_haves:
  truths:
    - "User can open config dialog and see all gate properties editable"
    - "Opening angle slider updates live preview in real-time"
    - "Color swatch taps open color picker and preview updates on change"
    - "Side selection toggles between left and right with preview updating"
    - "Timing fields accept open time and optional close time"
  artifacts:
    - path: "lib/page_creator/assets/conveyor_gate.dart"
      provides: "ConveyorGateConfigEditor widget with live preview"
      contains: "class ConveyorGateConfigEditor"
  key_links:
    - from: "ConveyorGateConfigEditor"
      to: "PneumaticDiverterPainter"
      via: "live preview CustomPaint inside dialog"
      pattern: "PneumaticDiverterPainter"
    - from: "ConveyorGateConfigEditor"
      to: "flutter_colorpicker"
      via: "ColorPicker in showDialog"
      pattern: "ColorPicker"
    - from: "ConveyorGateConfig.configure()"
      to: "ConveyorGateConfigEditor"
      via: "showDialog returning editor"
      pattern: "showDialog"
---

<objective>
Build the configuration dialog for ConveyorGateConfig with live preview, color pickers, angle slider, side selector, timing inputs, and OPC UA key field.

Purpose: Operators need to configure gate properties visually. The live preview per user decision lets them see changes before committing.
Output: Complete ConveyorGateConfigEditor widget, ConveyorGateConfig.configure() wired to show it.
</objective>

<execution_context>
@/Users/jonb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-standalone-diverter-gate/01-CONTEXT.md
@.planning/phases/01-standalone-diverter-gate/01-RESEARCH.md
@.planning/phases/01-standalone-diverter-gate/01-01-SUMMARY.md

@lib/page_creator/assets/speedbatcher.dart
@lib/page_creator/assets/common.dart
@lib/page_creator/assets/conveyor_gate.dart
@lib/page_creator/assets/conveyor_gate_painter.dart

<interfaces>
<!-- From Plan 01 output -->

From lib/page_creator/assets/conveyor_gate.dart:
- ConveyorGateConfig: extends BaseAsset with variant, side, stateKey, openAngleDegrees, openTimeMs, closeTimeMs, openColor, closedColor
- GateVariant enum: pneumatic, slider, pusher
- GateSide enum: left, right

From lib/page_creator/assets/conveyor_gate_painter.dart:
- PneumaticDiverterPainter(progress: ValueNotifier<double>, stateColor: Color, openAngleDegrees: double, side: GateSide)

From lib/page_creator/assets/common.dart:
- KeyField: OPC UA key selector widget
- SizeField: width/height editor
- CoordinatesField: x, y, angle editor
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConveyorGateConfigEditor with live preview</name>
  <files>lib/page_creator/assets/conveyor_gate.dart</files>
  <action>
    In `conveyor_gate.dart`, create `ConveyorGateConfigEditor extends StatefulWidget` and its `_ConveyorGateConfigEditorState`:

    1. **Live preview section** (top of dialog):
       - SizedBox(width: 150, height: 150) containing CustomPaint with PneumaticDiverterPainter
       - Use a ValueNotifier<double> initialized to 0.5 (mid-transition) for preview progress
       - Add a small "Play" IconButton that runs a simple animation cycle (forward then reverse) using a local AnimationController with SingleTickerProviderStateMixin on the state class
       - Preview updates whenever user changes angle, colors, or side via setState

    2. **Config fields** (scrollable column below preview):
       - **OPC UA State Key**: KeyField widget for `config.stateKey` (required field)
       - **Gate Side**: SegmentedButton or DropdownButton for GateSide.left/right, wrapped in setState
       - **Opening Angle**: Slider(min: 0, max: 90) with Text showing current value as integer degrees, wrapped in setState
       - **Open Time**: TextFormField for openTimeMs (milliseconds), with input validation (positive int)
       - **Close Time**: TextFormField for closeTimeMs (nullable -- empty means same as open time), with hint text "Same as open time"
       - **Open Color**: Inline swatch (24x24 circle with border) + label "Open Color". GestureDetector onTap opens flutter_colorpicker ColorPicker in AlertDialog. On color change, setState to update config and preview. Pattern from GraphAsset.
       - **Close Color**: Same inline swatch pattern for closedColor

    3. **Dialog structure**:
       - Use showDialog with AlertDialog (or Dialog for more space -- Claude's discretion on width)
       - Title: "Configure Conveyor Gate"
       - Actions: Cancel (pop without saving) and OK (pop returning true/modified config)
       - Content wrapped in SingleChildScrollView for overflow safety

    4. **Wire configure() method**:
       - Update `ConveyorGateConfig.configure()` to call `showDialog` launching `ConveyorGateConfigEditor`
       - Follow the pattern from `GateStatusConfig.configure()` in speedbatcher.dart
       - Return the editor as a Widget (matching BaseAsset.configure() signature)

    5. **Color picker helper**:
       - Create `_showColorPicker(BuildContext context, Color current, ValueChanged<Color> onChanged)` method
       - Opens AlertDialog with SingleChildScrollView > ColorPicker
       - "Done" button pops dialog
       - Same pattern as GraphAsset/DrawnBox usage of flutter_colorpicker

    Important: Every field mutation MUST be wrapped in setState to update the live preview (per Pitfall 5 from RESEARCH.md).
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/tfc-hmi2 && flutter analyze lib/page_creator/assets/conveyor_gate.dart 2>&1 | tail -10</automated>
  </verify>
  <done>
    ConveyorGateConfig.configure() returns a config editor widget. Editor shows live preview with PneumaticDiverterPainter at mid-transition. All fields (key, side, angle slider, timing, color swatches) are present and wrapped in setState. Color swatches open flutter_colorpicker on tap. Preview updates in real-time as user changes any field.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Configuration dialog with live gate preview, angle slider (0-90), side selector, timing inputs, and color swatches using flutter_colorpicker. Preview updates in real-time as fields change.</what-built>
  <how-to-verify>
    1. Run the app: `flutter run`
    2. In page editor, add a "Conveyor Gate" asset from the asset list (if registry is wired in Plan 03, manually test the dialog by temporarily adding a test button)
    3. Open the configuration dialog
    4. Verify live preview shows the pneumatic diverter at mid-transition
    5. Drag the angle slider -- preview flap angle should change in real-time
    6. Toggle side selector -- preview should mirror the gate
    7. Tap the open color swatch -- color picker should appear, selecting a color updates preview
    8. Tap the close color swatch -- same behavior
    9. Change timing values -- fields accept input
    10. Click "Play" button on preview -- should see open/close animation cycle
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the dialog</resume-signal>
</task>

</tasks>

<verification>
- `flutter analyze lib/page_creator/assets/conveyor_gate.dart` -- no errors
- Config dialog opens and shows all fields
- Live preview renders and updates with field changes
</verification>

<success_criteria>
- ConveyorGateConfigEditor renders all config fields with live preview
- Color swatches open flutter_colorpicker dialogs
- Angle slider updates preview flap angle in real-time
- Side toggle mirrors preview geometry
- All field changes wrapped in setState for immediate preview update
</success_criteria>

<output>
After completion, create `.planning/phases/01-standalone-diverter-gate/01-02-SUMMARY.md`
</output>
