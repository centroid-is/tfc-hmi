---
phase: 17-fix-and-verify-umas-against-real-schneider-plc
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - packages/tfc_dart/test/umas_live_test.dart
  - packages/tfc_dart/lib/core/umas_client.dart
  - packages/tfc_dart/lib/core/umas_types.dart
autonomous: false
requirements: [VER-01, VER-02]

must_haves:
  truths:
    - "UmasClient.init() succeeds against real Schneider PLC at 10.50.10.123"
    - "UmasClient.browse() returns a non-empty variable tree from the real PLC"
    - "Live tests skip by default and run with --run-skipped"
    - "Any protocol mismatches discovered during live testing are fixed in umas_client.dart"
  artifacts:
    - path: "packages/tfc_dart/test/umas_live_test.dart"
      provides: "Live hardware integration tests for UMAS against real Schneider PLC"
      contains: "10.50.10.123"
    - path: "packages/tfc_dart/lib/core/umas_client.dart"
      provides: "UmasClient that works against real hardware (may have fixes from live testing)"
      contains: "readPlcId"
  key_links:
    - from: "packages/tfc_dart/test/umas_live_test.dart"
      to: "packages/tfc_dart/lib/core/umas_client.dart"
      via: "UmasClient.browse() called with real ModbusClientTcp.send"
      pattern: "UmasClient.*sendFn.*tcp.send"
    - from: "packages/tfc_dart/test/umas_live_test.dart"
      to: "10.50.10.123:502"
      via: "Real TCP connection to Schneider PLC"
      pattern: "10.50.10.123"
---

<objective>
Write live hardware integration tests that verify UmasClient works against the real Schneider PLC at 10.50.10.123. Fix any protocol mismatches discovered during live testing.

Purpose: Unit tests and stub server only verify our understanding of the protocol. Real hardware validation is the ground truth -- if the PLC rejects our requests or returns unexpected data, we fix the implementation.
Output: umas_live_test.dart with skip-by-default tests, plus any implementation fixes needed for real PLC compatibility.
</objective>

<execution_context>
@/Users/jonb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fix-and-verify-umas-against-real-schneider-plc/17-RESEARCH.md
@.planning/phases/17-fix-and-verify-umas-against-real-schneider-plc/17-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (will be available after execution) -->
From packages/tfc_dart/lib/core/umas_client.dart (after Plan 01 fixes):
```dart
class UmasClient {
  Future<UmasPlcIdent> readPlcId();   // NEW: 0x02 Read PLC ID
  Future<UmasInitResult> init();       // Existing: 0x01 Init
  Future<List<UmasVariable>> readVariableNames();  // Fixed: paginated, corrected format
  Future<List<UmasDataTypeRef>> readDataTypes();    // Fixed: paginated, corrected format
  Future<List<UmasVariableTreeNode>> browse();      // Fixed: readPlcId -> init -> read DD
}
```

From packages/tfc_dart/lib/core/umas_types.dart (after Plan 01 fixes):
```dart
class UmasPlcIdent {          // NEW
  final int hardwareId;
  final int index;
  final int numberOfMemoryBanks;
}
```

From packages/tfc_dart/test/modbus_live_test.dart (existing pattern):
```dart
const _host = '10.50.10.152';
const _port = 502;
// Tests use: skip: 'Manual test -- run with --run-skipped'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create live UMAS integration tests and fix any real PLC issues</name>
  <files>
    packages/tfc_dart/test/umas_live_test.dart,
    packages/tfc_dart/lib/core/umas_client.dart,
    packages/tfc_dart/lib/core/umas_types.dart
  </files>
  <action>
    Create live hardware integration tests following the modbus_live_test.dart pattern. Then run them against the real PLC and fix any issues discovered.

    **Step 1 -- Create umas_live_test.dart:**

    Create `packages/tfc_dart/test/umas_live_test.dart` as a pure Dart test file (NOT Flutter):

    ```dart
    @TestOn('vm')
    library;
    ```

    Constants:
    - `_host = '10.50.10.123'`
    - `_port = 502`

    setUp: Create ModbusClientTcp with connectionTimeout 5s, connectionMode doNotConnect.
    tearDown: await tcp.disconnect().

    Test 1 -- `TCP connection to Schneider PLC succeeds`:
    - Connect, assert isConnected
    - skip: 'Live test -- requires Schneider PLC at $_host'

    Test 2 -- `readPlcId() returns valid hardwareId and index`:
    - Connect, create UmasClient, call readPlcId()
    - Print hardwareId, index, numberOfMemoryBanks for debugging
    - Assert hardwareId > 0
    - skip: 'Live test -- requires Schneider PLC at $_host'

    Test 3 -- `init() returns valid maxFrameSize`:
    - Connect, create UmasClient, call readPlcId() then init()
    - Print maxFrameSize
    - Assert maxFrameSize > 0
    - skip: 'Live test -- requires Schneider PLC at $_host'

    Test 4 -- `readDataTypes() returns data type list`:
    - Connect, create UmasClient, readPlcId(), init(), readDataTypes()
    - Print all data types with id, name, byteSize, classIdentifier
    - Assert data types list is not empty (real PLC should have at least built-in types)
    - skip: 'Live test -- requires Schneider PLC at $_host'

    Test 5 -- `readVariableNames() returns variable list`:
    - Connect, create UmasClient, readPlcId(), init(), readVariableNames()
    - Print all variables with name, blockNo, offset, dataTypeId
    - Assert variables list is not empty
    - skip: 'Live test -- requires Schneider PLC at $_host'

    Test 6 -- `browse() returns complete variable tree`:
    - Connect, create UmasClient, call browse()
    - Print full tree with depth indentation
    - Assert tree is not empty
    - Assert at least one leaf node has a non-null variable
    - skip: 'Live test -- requires Schneider PLC at $_host'
    - timeout: Timeout(Duration(seconds: 30))

    **Step 2 -- Run live tests and iterate:**

    Run: `cd /Users/jonb/Projects/tfc-hmi/packages/tfc_dart && dart test test/umas_live_test.dart --run-skipped --reporter expanded`

    EXPECT FAILURES. The PLC4X mspec is our best guess at the wire format, but the real PLC may differ in:
    - Exact byte offsets in 0x02 response (hardwareId location)
    - Header field sizes in 0x26 response
    - String null-termination behavior
    - Pagination thresholds and markers

    For each failure:
    1. Read the error output carefully -- print the raw PDU bytes (hex) for debugging
    2. Add hex dump debug output to the test if needed: `print('PDU: ${pdu.map((b) => b.toRadixString(16).padLeft(2, '0')).join(' ')}')`
    3. Fix the parsing in umas_client.dart to match real PLC behavior
    4. Re-run until green
    5. After fixing, update unit tests and stub server in umas_client_test.dart and umas_stub_server.py if the wire format turns out different than expected

    **Step 3 -- Verify no regressions:**
    After all live tests pass, run the full test suite to ensure unit tests and e2e tests still pass.

    **Important notes:**
    - Use `dart test` NOT `flutter test` per user requirement
    - All live tests use `skip:` parameter so they are skipped in normal CI runs
    - Print liberally in live tests -- this is debugging against real hardware, output is valuable
    - If the PLC does not have Data Dictionary enabled, test 4-6 will fail with a UMAS error. Document this in test output. The user may need to enable it on the PLC.
    - If 0x02 response format differs from PLC4X mspec, start by printing raw bytes and adjusting offsets
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/tfc-hmi/packages/tfc_dart && dart test test/umas_live_test.dart --run-skipped --reporter expanded</automated>
  </verify>
  <done>
    All 6 live tests pass against real Schneider PLC at 10.50.10.123. UmasClient.browse() returns a non-empty variable tree. Any protocol fixes from real PLC testing are committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify UMAS variable tree matches real PLC project</name>
  <action>
    Human verifies that the UMAS browse output from the live tests matches the actual Schneider PLC project variables.

    What was built: UMAS protocol client tested against real Schneider PLC at 10.50.10.123. Live integration tests created and passing. All protocol fixes from Phase 14 bugs applied and verified against real hardware.

    How to verify:
    1. Review the live test output: `cd /Users/jonb/Projects/tfc-hmi/packages/tfc_dart && dart test test/umas_live_test.dart --run-skipped --reporter expanded`
    2. Verify the variable tree printed in the browse() test looks correct for your Schneider PLC project
    3. Verify variable names, data types, and addresses match what you see in Unity Pro / EcoStruxure
    4. Run full test suite to confirm no regressions: `cd /Users/jonb/Projects/tfc-hmi/packages/tfc_dart && dart test --reporter compact`
    5. Optionally: Open the HMI app, connect to the Schneider PLC, and try UMAS browse from the key repository UI to see if the variables appear

    Resume signal: Type "approved" if variable tree looks correct, or describe any issues with variable names/types/addresses.
  </action>
  <verify>Human confirms variable tree matches PLC project</verify>
  <done>Human approves that UMAS browse returns correct variable names, data types, and addresses matching the real Schneider PLC project</done>
</task>

</tasks>

<verification>
1. `cd /Users/jonb/Projects/tfc-hmi/packages/tfc_dart && dart test test/umas_live_test.dart --run-skipped --reporter expanded` -- all live tests pass
2. `cd /Users/jonb/Projects/tfc-hmi/packages/tfc_dart && dart test --reporter compact` -- full suite green, no regressions
3. `dart analyze packages/tfc_dart` -- no analysis errors
4. Human verification: variable tree matches PLC project
</verification>

<success_criteria>
- umas_live_test.dart exists with 6 skip-by-default tests
- All live tests pass with --run-skipped against 10.50.10.123
- UmasClient.init() returns valid maxFrameSize from real PLC
- UmasClient.browse() returns non-empty variable tree from real PLC
- Any protocol adjustments from live testing are committed and unit tests updated
- Full dart test suite passes (unit + e2e + live)
- Human approves variable tree correctness
</success_criteria>

<output>
After completion, create `.planning/phases/17-fix-and-verify-umas-against-real-schneider-plc/17-02-SUMMARY.md`
</output>
