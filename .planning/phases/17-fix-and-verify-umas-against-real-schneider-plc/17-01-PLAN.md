---
phase: 17-fix-and-verify-umas-against-real-schneider-plc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tfc_dart/lib/core/umas_client.dart
  - packages/tfc_dart/lib/core/umas_types.dart
  - packages/tfc_dart/test/core/umas_client_test.dart
  - test/umas_stub_server.py
  - packages/tfc_dart/test/umas_e2e_test.dart
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03, FIX-04, FIX-05, FIX-06]

must_haves:
  truths:
    - "0x26 request sends 13-byte payload (recordType + index + hardwareId + blockNo + offset + blank)"
    - "readPlcId() calls 0x02 and extracts hardwareId and memory block index"
    - "browse() calls 0x02 before 0x01, passes hardwareId/index to 0x26"
    - "Paginated 0x26 reads loop until nextAddress == 0"
    - "0x26 response header (range + nextAddress + noOfRecords) parsed before records"
    - "Variable records parsed as dataType(2) + block(2) + offset(2) + unknown(2) + strLen(2) + name"
    - "Data type records parsed as dataSize(2) + unknown(2) + classId(1) + dataType(1) + strLen(1) + name"
  artifacts:
    - path: "packages/tfc_dart/lib/core/umas_client.dart"
      provides: "Fixed UmasClient with readPlcId, pagination, corrected 0x26 format"
      contains: "readPlcId"
    - path: "packages/tfc_dart/lib/core/umas_types.dart"
      provides: "UmasPlcIdent type, updated UmasDataTypeRef with classIdentifier"
      contains: "UmasPlcIdent"
    - path: "packages/tfc_dart/test/core/umas_client_test.dart"
      provides: "Unit tests matching corrected wire format"
      contains: "readPlcId"
    - path: "test/umas_stub_server.py"
      provides: "Stub server matching corrected protocol format"
      contains: "0x02"
    - path: "packages/tfc_dart/test/umas_e2e_test.dart"
      provides: "E2E tests updated for corrected protocol"
      contains: "readPlcId"
  key_links:
    - from: "packages/tfc_dart/lib/core/umas_client.dart"
      to: "packages/tfc_dart/lib/core/umas_types.dart"
      via: "UmasPlcIdent type used by readPlcId return"
      pattern: "UmasPlcIdent"
    - from: "packages/tfc_dart/lib/core/umas_client.dart"
      to: "packages/tfc_dart/lib/core/umas_types.dart"
      via: "UmasDataTypeRef.classIdentifier used for UDT detection"
      pattern: "classIdentifier"
    - from: "packages/tfc_dart/test/core/umas_client_test.dart"
      to: "packages/tfc_dart/lib/core/umas_client.dart"
      via: "MockUmasSender canned responses match corrected wire format"
      pattern: "buildVariableNamesPayload"
---

<objective>
Fix all six UMAS protocol bugs identified by comparing against the PLC4X mspec. The current implementation was built against a simplified stub server and sends incorrect request payloads, missing the 0x02 init step, pagination, and using wrong record field order.

Purpose: Without these fixes, UmasClient cannot communicate with a real Schneider PLC -- requests are rejected or responses are misparsed.
Output: Corrected umas_client.dart, umas_types.dart, updated unit tests, updated stub server, updated e2e tests.
</objective>

<execution_context>
@/Users/jonb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fix-and-verify-umas-against-real-schneider-plc/17-RESEARCH.md

<interfaces>
<!-- Current UmasClient API (to be modified, not replaced) -->
From packages/tfc_dart/lib/core/umas_client.dart:
```dart
class UmasRequest extends ModbusRequest {
  final int umasSubFunction;
  final int pairingKey;
  final Uint8List umasPayload;
  // ... PDU construction for FC90
}

class UmasClient {
  final Future<ModbusResponseCode> Function(ModbusRequest request) sendFn;
  int _pairingKey = 0x00;
  int? maxFrameSize;

  Future<UmasInitResult> init();
  Future<List<UmasVariable>> readVariableNames();
  Future<List<UmasDataTypeRef>> readDataTypes();
  List<UmasVariableTreeNode> buildVariableTree(List<UmasVariable>, List<UmasDataTypeRef>);
  Future<List<UmasVariableTreeNode>> browse();
}
```

From packages/tfc_dart/lib/core/umas_types.dart:
```dart
enum UmasSubFunction { init(0x01), readId(0x02), readProjectInfo(0x03), readDataDictionary(0x26); }
class UmasVariable { String name; int blockNo; int offset; int dataTypeId; }
class UmasDataTypeRef { int id; String name; int byteSize; }
class UmasVariableTreeNode { String name; String path; List<UmasVariableTreeNode> children; UmasVariable? variable; UmasDataTypeRef? dataType; }
class UmasException { int errorCode; String message; }
class UmasInitResult { int maxFrameSize; String? firmwareVersion; }
class UmasDataTypes { static const Map<int, UmasDataTypeRef> builtIn; static UmasDataTypeRef? resolve(int id, List<UmasDataTypeRef> customTypes); }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix UMAS protocol types and unit tests (RED then GREEN)</name>
  <files>
    packages/tfc_dart/lib/core/umas_types.dart,
    packages/tfc_dart/lib/core/umas_client.dart,
    packages/tfc_dart/test/core/umas_client_test.dart
  </files>
  <behavior>
    - readPlcId() sends sub-function 0x02 and parses response to extract hardwareId (uint32) and index (uint8 from memory block array)
    - UmasPlcIdent type holds hardwareId, index, and numberOfMemoryBanks
    - UmasDataTypeRef gains classIdentifier (uint8) and dataType (uint8) fields alongside existing id/name/byteSize
    - _build0x26Payload() creates 13-byte payload: recordType(2 LE) + index(1) + hardwareId(4 LE) + blockNo(2 LE) + offset(2 LE) + blank(2 LE)
    - readVariableNames() and readDataTypes() paginate: loop while (nextAddress != 0 || firstMessage)
    - 0x26 response header parsed: range(1) + nextAddress(2) + unknown(1-2) + noOfRecords(2) before records
    - Variable record: dataType(2 LE) + block(2 LE) + offset(2 LE) + unknown4(2 LE) + stringLength(2 LE) + name (null-terminated)
    - Data type record: dataSize(2 LE) + unknown(2 LE) + classIdentifier(1) + dataType(1) + stringLength(1) + name (null-terminated)
    - browse() sequence: readPlcId() -> init() -> readDataTypes(paginated) -> readVariableNames(paginated) -> buildVariableTree()
    - Pagination test: MockUmasSender returns 2 pages for DD02 (nextAddress != 0 on first, 0 on second), client accumulates both
    - Single-page test: MockUmasSender returns all data in one response (nextAddress == 0), client handles correctly
  </behavior>
  <action>
    IMPORTANT: Follow TDD. Write/update tests FIRST (RED), then implement (GREEN).

    **Step 1 — Update types (umas_types.dart):**
    - Add `UmasPlcIdent` class with fields: `int hardwareId`, `int index`, `int numberOfMemoryBanks`
    - Add `classIdentifier` (int) and `dataType` (int) fields to `UmasDataTypeRef`. Keep existing `id`, `name`, `byteSize` fields. Make classIdentifier and dataType optional with default 0 for backward compatibility with existing code that constructs UmasDataTypeRef without them.

    **Step 2 — Update unit tests (RED):**
    Rewrite `umas_client_test.dart` test helpers and tests to match corrected wire format:

    - Update `MockUmasSender` to support returning different responses per call to the same sub-function (needed for pagination -- first 0x26 returns page 1, second 0x26 returns page 2). Use a queue: `Map<int, List<Uint8List>>` where each sub-function has a list of responses consumed in order.
    - Update `buildVariableNamesPayload()` to build the CORRECTED DD02 response format:
      Header: range(1) + nextAddress(2 LE) + unknown1(2 LE) + noOfRecords(2 LE)
      Records: dataType(2 LE) + block(2 LE) + offset(2 LE) + unknown4(2 LE) + stringLength(2 LE) + name(N bytes, null-terminated)
    - Update `buildDataTypesPayload()` to build the CORRECTED DD03 response format:
      Header: range(1) + nextAddress(2 LE) + unknown1(1) + noOfRecords(2 LE)
      Records: dataSize(2 LE) + unknown1(2 LE) + classIdentifier(1) + dataType(1) + stringLength(1) + name(N bytes, null-terminated)
    - Add `buildPlcIdentResponse()` helper for 0x02 response with hardwareId and memory blocks
    - Add test: `readPlcId() parses 0x02 response to extract hardwareId and index`
    - Add test: `readVariableNames() paginates when nextAddress != 0` -- mock returns 2 pages
    - Add test: `readDataTypes() paginates when nextAddress != 0` -- mock returns 2 pages
    - Update existing variable record parsing test to use new field order
    - Update existing data type parsing test to use new field order with classIdentifier/dataType
    - Add test: `browse() calls readPlcId before init` -- verify sub-function call order from sentRequests
    - Add test: `0x26 request payload is 13 bytes with correct fields` -- inspect sentRequests payload
    - Run tests -- they should FAIL (RED).

    **Step 3 — Fix implementation (GREEN):**
    Update `umas_client.dart`:

    - Add `int? _hardwareId` and `int? _index` fields to UmasClient
    - Add `Future<UmasPlcIdent> readPlcId()` method:
      - Sends UmasRequest with subFunc 0x02
      - Parses response to extract hardwareId (uint32 LE) and memory block info
      - Stores _hardwareId and _index for use by 0x26 requests
      - Returns UmasPlcIdent
    - Add `Uint8List _build0x26Payload({required int recordType, required int blockNo, required int offset})` private method:
      - Creates 13-byte payload: recordType(2 LE) + _index!(1) + _hardwareId!(4 LE) + blockNo(2 LE) + offset(2 LE) + blank(2 LE)
    - Rewrite `readVariableNames()` with pagination:
      - Loop: send 0x26 with DD02, parse response header (range, nextAddress, noOfRecords), parse records, continue until nextAddress == 0
      - First request: blockNo=0xFFFF, offset=0x0000
      - Subsequent: blockNo=0xFFFF, offset=nextAddress from previous response
    - Rewrite `readDataTypes()` with pagination:
      - Same loop pattern but DD03
      - First request: blockNo=0x0000, offset=0x0000
      - Subsequent: blockNo=nextAddress from previous response, offset=0x0000
    - Rewrite `_parseVariableRecords()` for corrected field order:
      - Parse header: skip range(1), read nextAddress(2 LE), skip unknown1(2 LE), read noOfRecords(2 LE)
      - Parse each record: dataType(2 LE) + block(2 LE) + offset(2 LE) + unknown4(2 LE) + stringLength(2 LE) + name
      - Strip trailing null bytes from name
      - Return (nextAddress, List<UmasVariable>)
    - Rewrite `_parseDataTypeRecords()` for corrected field order:
      - Parse header: skip range(1), read nextAddress(2 LE), skip unknown1(1), read noOfRecords(2 LE)
      - Parse each record: dataSize(2 LE) + unknown1(2 LE) + classIdentifier(1) + dataType(1) + stringLength(1) + name
      - Strip trailing null bytes from name
      - Return (nextAddress, List<UmasDataTypeRef>)
    - Update `browse()` sequence: readPlcId() -> init() -> readDataTypes(paginated) -> readVariableNames(paginated) -> buildVariableTree()
    - Run tests -- they should PASS (GREEN).

    **Important notes:**
    - The 0x02 response format from PLC4X mspec has: range(2) + ident/hardwareId(4) + ... + numberOfMemoryBanks(1) + PlcMemoryBlockIdent[]. Start with a simplified parse that extracts hardwareId from bytes 4-7 (after range) and index from the first memory block entry. The exact offsets may need adjustment based on real PLC output -- the live test in Plan 02 will validate this.
    - For string parsing: read stringLength bytes, then strip any trailing 0x00 bytes.
    - Keep _maxNameLength and _maxVariables safety limits.
    - Retain error handling patterns (0xFD status check, null/short PDU checks).
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/tfc-hmi/packages/tfc_dart && dart test test/core/umas_client_test.dart --reporter compact</automated>
  </verify>
  <done>
    All unit tests pass with corrected wire format: 13-byte 0x26 payload, readPlcId extracts hardwareId/index, pagination accumulates multi-page responses, variable records parsed dataType-first, data type records parsed with classIdentifier/dataType fields, browse calls readPlcId before init.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update stub server and e2e tests for corrected protocol</name>
  <files>
    test/umas_stub_server.py,
    packages/tfc_dart/test/umas_e2e_test.dart
  </files>
  <action>
    Update the Python stub server and Dart e2e tests to match the corrected UMAS protocol format so the full TCP integration tests pass.

    **Step 1 — Update stub server (test/umas_stub_server.py):**

    Add 0x02 (Read PLC Identification) handler:
    - Return a response with: range(2 bytes, e.g. 0x0001) + ident/hardwareId(4 bytes, e.g. 0x12345678 LE) + padding to reach numberOfMemoryBanks field + numberOfMemoryBanks(1, e.g. 1) + one PlcMemoryBlockIdent entry with address(2) + blockType(1) + unknown(2) + memoryLength(4). Use index=0 in the memory block.
    - The exact byte layout should match what readPlcId() in umas_client.dart expects. Start with a minimal response that provides the hardwareId and index values the client needs.

    Update 0x26 handler for corrected format:
    - Accept and validate the full 13-byte payload (not just 2-byte record type)
    - For DD02 responses: build header (range=0, nextAddress=0x0000 for single page, unknown1=0x0000, noOfRecords=len(VARIABLES)) + variable records in corrected format: dataType(2 LE) + block(2 LE) + offset(2 LE) + unknown4(2 LE, 0x0000) + stringLength(2 LE) + name (null-terminated)
    - For DD03 responses: build header (range=0, nextAddress=0x0000, unknown1=0x00, noOfRecords=len(DATA_TYPES)) + data type records in corrected format: dataSize(2 LE) + unknown1(2 LE, 0x0000) + classIdentifier(1) + dataType(1) + stringLength(1) + name (null-terminated)
    - Add pagination support: accept an optional page_size parameter (default: return all in one page). If data exceeds page_size, return first batch with nextAddress pointing to continuation, then return remainder on next request.

    Update init (0x01) handler if needed:
    - Keep existing behavior (return maxFrameSize=1024).
    - Store pairing key from request for use in subsequent responses.

    **Step 2 — Update e2e tests (packages/tfc_dart/test/umas_e2e_test.dart):**

    The e2e test connects via real ModbusClientTcp to the stub server. Update to match the new browse() flow:
    - browse() now calls readPlcId() (0x02) then init() (0x01) then data dictionary reads
    - The existing test assertions for tree structure (Application root, GVL/Motor/Counters children, leaf variables) should remain valid since the stub data hasn't changed
    - Update the `readVariableNames returns all 10 variables` test: it now requires readPlcId() and init() to be called first (since readVariableNames uses _hardwareId and _index). Either call browse() which does the full sequence, or call readPlcId() then init() then readVariableNames().
    - Update the `readDataTypes returns custom types` test similarly.
    - Update `init() returns maxFrameSize` test: now readPlcId() must be called first if browse() requires it. But init() itself should still work standalone -- verify this.
    - Add assertion that browse tree still has correct variable data (blockNo, offset, dataType) after format changes.

    **Step 3 — Run full test suite:**
    - Run e2e tests to verify stub server + client communicate correctly
    - Run full dart test suite to check no regressions
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/tfc-hmi/packages/tfc_dart && dart test test/umas_e2e_test.dart --reporter compact && dart test --reporter compact</automated>
  </verify>
  <done>
    Stub server handles 0x02, corrected 0x26 format, and pagination. E2e tests pass with full browse flow (readPlcId -> init -> readDataTypes -> readVariableNames -> tree). Full dart test suite passes with no regressions.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/jonb/Projects/tfc-hmi/packages/tfc_dart && dart test test/core/umas_client_test.dart --reporter compact` -- all unit tests green
2. `cd /Users/jonb/Projects/tfc-hmi/packages/tfc_dart && dart test test/umas_e2e_test.dart --reporter compact` -- e2e against stub green
3. `cd /Users/jonb/Projects/tfc-hmi/packages/tfc_dart && dart test --reporter compact` -- full suite green, no regressions
4. `dart analyze packages/tfc_dart` -- no analysis errors
</verification>

<success_criteria>
- UmasClient.readPlcId() exists and parses 0x02 response for hardwareId + index
- UmasClient sends 13-byte 0x26 payload (not 2-byte)
- Pagination loop implemented for both DD02 and DD03 reads
- Variable record parsing uses corrected field order (dataType first, name last)
- Data type record parsing uses corrected field order with classIdentifier
- browse() sequence is readPlcId -> init -> readDataTypes -> readVariableNames -> buildTree
- All existing unit tests updated and passing
- Stub server updated to match corrected protocol
- E2e tests pass against updated stub
- Full dart test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/17-fix-and-verify-umas-against-real-schneider-plc/17-01-SUMMARY.md`
</output>
