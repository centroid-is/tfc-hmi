---
phase: 17-fix-and-verify-umas-against-real-schneider-plc
task: post-phase
total_tasks: 2
status: in_progress
last_updated: 2026-03-10T07:02:11.811Z
---

<current_state>
Phase 17 plans (17-01, 17-02) are complete and committed. Phase verification found gaps (VER-01, VER-02) because the M241 PLC does not support UMAS — this is expected and not a code issue. Post-phase manual improvements are in progress: UMAS error UI, type coercion fix, and live StateMan tests are committed. Two remaining items requested by user: unit ID info helper and endianness helper in server config UI.
</current_state>

<completed_work>

- Plan 17-01: Fix UMAS wire format bugs (6 fixes from PLC4X mspec comparison) — 4 commits
- Plan 17-02: Live hardware tests against real Schneider PLC — 2 commits
- Phase verification: gaps_found (VER-01/VER-02 — M241 doesn't support UMAS, not a code bug)
- Post-phase: UMAS error help dialog in browse_panel with "How to fix this" button (0x83 → Data Dictionary steps)
- Post-phase: ModbusClientWrapper._coerceValue() fix — library returns double for int types, now coerced to int
- Post-phase: Live ModbusClientWrapper + DeviceClientAdapter tests against M241 (7 tests, all passing)
- Post-phase: UMAS toggle subtitle updated to "M340/M580 only"
- All committed as `49ea117`
</completed_work>

<remaining_work>

- Add unit ID info button to server_config.dart — similar to existing endianness info IconButton. Research common PLC vendor defaults for unit ID (Schneider, Siemens, Allen-Bradley, ABB, etc.)
- Endianness helper — user said "I am still missing endianness helper". The dropdown + info tooltip already exist in server_config.dart (line 1881-1927). Clarify what's missing — could be: auto-detect button, live preview, or something else. Ask user.
</remaining_work>

<decisions_made>

- M241 confirmed NOT supporting UMAS (FC90 returns 0x83 for all sub-functions regardless of unit ID)
- Schneider M241 uses CDAB (word-swapped) byte order for 32-bit REAL values — confirmed via raw TCP test
- Unit ID doesn't matter for M241 Modbus TCP (tested 0, 1, 254, 255 — all work identically)
- BrowseErrorMapper pattern keeps browse_panel.dart protocol-agnostic while allowing UMAS-specific error help
- Type coercion done in wrapper (not adapter) since it's a modbus_client library issue affecting all consumers
</decisions_made>

<blockers>
- None. PLC at 10.50.10.123 is accessible and responding to standard Modbus.
</blockers>

<context>
User wants an info button next to the Unit ID field in server_config.dart, showing common PLC vendor defaults — same pattern as the existing endianness info button (line 1913-1927). Need to research what unit IDs are standard for Schneider, Siemens, Allen-Bradley, ABB, Omron, Wago, Danfoss, Beckhoff, etc.

Key files:
- server_config.dart: Unit ID field is around line 1550-1555 (TextEditingController), rendered in the form
- modbus_client_wrapper.dart: _coerceValue() added at line ~775
- browse_panel.dart: BrowseErrorMapper, _showErrorHelpDialog
- umas_browse.dart: _umasErrorMapper with 0x83 handling
- modbus_stateman_live_test.dart: 7 live tests against M241
</context>

<next_action>
1. Research common PLC vendor unit ID defaults (web search)
2. Add unit ID info IconButton in server_config.dart next to the unit ID text field, matching the endianness info button pattern
3. Clarify with user what endianness helper they're missing (the dropdown already exists)
4. Commit
</next_action>
