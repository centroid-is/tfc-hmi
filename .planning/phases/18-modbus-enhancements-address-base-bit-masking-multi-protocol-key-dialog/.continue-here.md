---
phase: 18-modbus-enhancements-address-base-bit-masking-multi-protocol-key-dialog
task: deduplicate-key-mapping-ui
total_tasks: 4
status: in_progress
last_updated: 2026-03-11T20:22:31.520Z
---

<current_state>
Deduplicated key mapping config UI — extracted shared section widgets from key_repository into reusable file, rewrote KeyMappingEntryDialog to use them. Added BitMaskGrid to dialog (was missing). Added single-bit selection mode for bit data types (required, always visible). All code compiles clean (0 errors). Not yet committed.

Also diagnosed `schneider.bool_test` LED issue: HR1050=22 (binary 0000000000010110), bit 0 is always false. User needs to select a different bit or register.
</current_state>

<completed_work>

- Phase 18 plans 01-03 and verification: all previously complete
- Code review fixes 1-3: previously complete (committed as c396503)
- NEW: Extracted `_OpcUaConfigSection`, `_ModbusConfigSection`, `_M2400ConfigSection`, `_CollectionConfigSection` from `key_repository.dart` → `lib/widgets/key_mapping_sections.dart` (public)
- NEW: Updated `key_repository.dart` — removed ~810 lines, imports shared widgets
- NEW: Rewrote `KeyMappingEntryDialog` in `common.dart` — uses shared section widgets, simplified state to single `KeyMappingEntry` with `onChanged` callbacks
- NEW: Added `BitMaskGrid` to dialog (previously missing entirely)
- NEW: Added `singleBit` param to `BitMaskGrid` — radio-button behavior for bit types
- NEW: Bit types show grid inline (required, always visible), non-bit types keep collapsible optional mask
- NEW: Dialog validates bit selection required before OK for bit data types
</completed_work>

<remaining_work>

- Commit all changes (4 files modified, 1 new file)
- Manual verification on device: key repository sections render/save, dialog works for all protocols + bit mask
- schneider.bool_test: user config issue (bit 0 not set), not a code bug
</remaining_work>

<decisions_made>

- Shared widgets keep Card-wrapped layout with FontAwesome icons from key_repository
- Dialog gets `SizedBox(width: 500)` for better layout with card-style sections
- `ValueKey` on section widgets keyed by `$protocol-$serverAlias` forces fresh state on server switch
- `ModbusConfigListExt` stays public in key_repository.dart, imported by key_mapping_sections.dart
- Sample expression UI (dialog-specific) kept inline, not in shared CollectionConfigSection
- Bit type: always-visible single-bit picker with validation; non-bit: collapsible optional multi-bit mask
</decisions_made>

<blockers>
None
</blockers>

<context>
Files modified (uncommitted):
- `lib/widgets/key_mapping_sections.dart` — NEW shared widget file
- `lib/widgets/bit_mask_grid.dart` — added `singleBit` parameter
- `lib/pages/key_repository.dart` — removed 4 widget classes, imports shared
- `lib/page_creator/assets/common.dart` — rewrote dialog to use shared widgets + BitMaskGrid

Pre-existing test failure (widget_test.dart missing locationBuilder param) is unrelated.
</context>

<next_action>
Start with: commit changes, then manual test if desired.
</next_action>
