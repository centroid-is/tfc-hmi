---
phase: 05-advantysstbstack-composite-parent
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - lib/page_creator/assets/advantys_stb.dart
  - test/page_creator/assets/advantys_stb_test.dart
  - test/page_creator/assets/goldens/advantys_stb/stack_full_light.png
  - test/page_creator/assets/goldens/advantys_stb/stack_full_dark.png
autonomous: true
requirements:
  - STACK-04
  - QUAL-06
  - QUAL-07
tags:
  - composite
  - configure-dialog
  - reorder
  - integration
  - goldens
must_haves:
  truths:
    - "Operator opens the stack's configure dialog and sees: Size field, Coordinates field (enableAngle: true), and a Subdevices manager"
    - "The Add picker offers exactly the 4 STB module types (NIP2311 / PDT3100 / DDI3725 / DDO3705) — no other type is selectable"
    - "Operator can drag-reorder the subdevices list via ReorderableListView"
    - "Operator can remove a subdevice via the trailing delete IconButton (no confirmation — CX5010 parity per CONTEXT.md verbatim-mirror commitment, deviating from CONTEXT §Configure Dialog phrasing. Post-execution verification required from the user — see SUMMARY)"
    - "The stack itself has NO `nameOrId` field (CX5010 parity — leaf modules carry their own. Deviates from CONTEXT §Specifics implication. Post-execution verification required from the user — see SUMMARY)"
    - "Tapping a list entry opens that subdevice's own configure dialog"
    - "An integration test pumps a stack containing 1× NIP + 1× PDT + 1× DDI + 1× DDO without throwing"
    - "The integration test asserts taps on DDI's and DDO's bodies fire their gesture handlers; taps on NIP/PDT areas do NOT throw (decorative, no GestureDetector — per RESEARCH finding 4)"
    - "Goldens `stack_full_light.png` and `stack_full_dark.png` exist at test/page_creator/assets/goldens/advantys_stb/ and match the canonical NIP+PDT+DDI+DDO layout"
    - "`flutter analyze` reports zero issues across all new/modified files (QUAL-06 final gate)"
  artifacts:
    - path: "lib/page_creator/assets/advantys_stb.dart"
      provides: "Real _AdvantysSTBStackConfigContent dialog (replaces Plan 01 stub) — Size + Coordinates fields + filtered Add dropdown + ReorderableListView + delete IconButton"
      contains: "class _AdvantysSTBStackConfigContent"
    - path: "test/page_creator/assets/advantys_stb_test.dart"
      provides: "Configure dialog widget tests + QUAL-07 integration test + goldens group"
      contains: "AdvantysSTBStack full-stack integration"
    - path: "test/page_creator/assets/goldens/advantys_stb/stack_full_light.png"
      provides: "Light-theme canonical 4-module stack golden"
      contains: ""
    - path: "test/page_creator/assets/goldens/advantys_stb/stack_full_dark.png"
      provides: "Dark-theme canonical 4-module stack golden"
      contains: ""
  key_links:
    - from: "AdvantysSTBStackConfig.configure"
      to: "_AdvantysSTBStackConfigContent"
      via: "SizedBox(width: 800, height: 500, child: _AdvantysSTBStackConfigContent(config: this))"
      pattern: "_AdvantysSTBStackConfigContent\\(config: this\\)"
    - from: "_AdvantysSTBStackConfigContent dropdown"
      to: "_availableSTBSubdevices Map (from Plan 01)"
      via: "DropdownButtonFormField iterates _availableSTBSubdevices.keys"
      pattern: "_availableSTBSubdevices\\.keys"
    - from: "Integration test"
      to: "Each STB leaf's GestureDetector (DDI+DDO only)"
      via: "tester.tap on DDI/DDO bodies; verify no-exception on NIP/PDT taps"
      pattern: "STBDDI3725Widget|STBDDO3705Widget"
---

<objective>
Replace the Plan-01 stub `configure()` with the real dialog `_AdvantysSTBStackConfigContent` — a verbatim clone of `_CXxxxxConfigContent` (lib/page_creator/assets/beckhoff.dart:136-285) with TWO substitutions: (a) `_availableSubdevices` → `_availableSTBSubdevices` (defined in Plan 01); (b) `enableAngle: true` on the Coordinates field (CX5010 parity per RESEARCH Q3). NO `nameOrId` field on the stack (CX5010 parity per CONTEXT — note this deviates from CONTEXT §Specifics implication; verbatim-mirror takes precedence; user must confirm post-execution). NO delete confirmation (CX5010 parity per RESEARCH Pattern 4 — note this deviates from CONTEXT §Configure Dialog "with confirmation" phrasing; verbatim-mirror takes precedence; user must confirm post-execution). Add a configure-dialog widget test (add / reorder / remove flows), the QUAL-07 full-stack integration test (1× of each module + tap routing assertions scoped to DDI+DDO per RESEARCH finding 4), and the two goldens (`stack_full_light.png` + `stack_full_dark.png`, macOS-gated, `--update-goldens` generated).

Purpose: This is the integration-and-render plan. STACK-04 (filtered dropdown + reorder + delete) and QUAL-07 (full-stack integration test + render check + tap routing) both land here. The Plan-01 data layer is in place; this plan closes the loop on the user-facing surface.

Output: Working configure dialog, passing widget tests for the add/reorder/remove flows, a passing integration test for the canonical 4-module stack, two passing goldens (generated locally with `--update-goldens` on macOS dev hardware), and a final `flutter analyze` clean across the whole Phase 5 footprint.

CONTEXT deviations the user must confirm post-execution (surfaced in SUMMARY per checker iteration 1 warnings #2 and #3):
  1. **No delete confirmation** — CX5010 parity wins. CONTEXT §Configure Dialog says delete "with confirmation"; CONTEXT also says "Mirrors `BeckhoffCX5010Config` verbatim" (stronger commitment). RESEARCH Assumption A4 flagged this.
  2. **No `nameOrId` on the stack** — CX5010 parity wins. CONTEXT §Specifics implies the stack should have a `nameOrId`; the four leaf modules carry their own. RESEARCH Open Question Q2 flagged this.
Both deviations are recorded as truths in must_haves with explicit "post-execution verification required" callouts; SUMMARY must list both as user-decision items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-advantysstbstack-composite-parent/05-CONTEXT.md
@.planning/phases/05-advantysstbstack-composite-parent/05-RESEARCH.md
@.planning/phases/05-advantysstbstack-composite-parent/05-01-SUMMARY.md

@lib/page_creator/assets/beckhoff.dart
@lib/page_creator/assets/advantys_stb.dart
@test/page_creator/assets/advantys_stb_test.dart

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From lib/page_creator/assets/beckhoff.dart:136-285 — the configure-dialog source to mirror.
The shape (executor reproduces verbatim, substituting `_availableSubdevices` → `_availableSTBSubdevices`
and `BeckhoffCX5010Config` → `AdvantysSTBStackConfig`):

```dart
class _CXxxxxConfigContent extends StatefulWidget {
  final BeckhoffCX5010Config config;
  const _CXxxxxConfigContent({required this.config});
  @override
  State<_CXxxxxConfigContent> createState() => _CXxxxxConfigContentState();
}

class _CXxxxxConfigContentState extends State<_CXxxxxConfigContent> {
  @override
  Widget build(BuildContext context) {
    return Row(
      crossAxisAlignment: CrossAxisAlignment.stretch,
      children: [
        // LEFT: SizeField + CoordinatesField(enableAngle: true)
        Expanded(
          child: Padding(
            padding: const EdgeInsets.all(20),
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Text('CX5010', style: Theme.of(context).textTheme.titleMedium),
                const SizedBox(height: 12),
                SizeField(initialValue: widget.config.size, onChanged: (size) => widget.config.size = size),
                const SizedBox(height: 16),
                CoordinatesField(initialValue: widget.config.coordinates, onChanged: (c) => widget.config.coordinates = c, enableAngle: true),
              ],
            ),
          ),
        ),
        const VerticalDivider(width: 1),
        // RIGHT: Subdevices manager (DropdownButtonFormField + ReorderableListView + delete)
        Expanded(
          child: Padding(
            padding: const EdgeInsets.all(20),
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Row(children: [
                  Text('Subdevices', style: Theme.of(context).textTheme.titleMedium),
                  const Spacer(),
                  FilledButton.icon(onPressed: () => Navigator.of(context).maybePop(), icon: const Icon(Icons.check), label: const Text('Done')),
                ]),
                const SizedBox(height: 8),
                DropdownButtonFormField<String>(
                  decoration: const InputDecoration(border: OutlineInputBorder(), labelText: 'Add Subdevice'),
                  value: null,
                  hint: const Text('Select a subdevice to add'),
                  items: _availableSubdevices.keys.map((k) => DropdownMenuItem(value: k, child: Text(k))).toList(),
                  onChanged: (v) {
                    if (v == null) return;
                    setState(() {
                      final mk = _availableSubdevices[v]!;
                      widget.config.subdevices.add(mk());
                    });
                  },
                ),
                const SizedBox(height: 16),
                if (widget.config.subdevices.isEmpty)
                  Expanded(child: Center(child: Text('No subdevices yet', style: Theme.of(context).textTheme.bodyMedium)))
                else ...[
                  Row(children: [
                    Text('Current Subdevices', style: Theme.of(context).textTheme.titleSmall),
                    const SizedBox(width: 8),
                    Chip(label: Text('${widget.config.subdevices.length}')),
                  ]),
                  const SizedBox(height: 8),
                  Expanded(
                    child: Card(
                      clipBehavior: Clip.antiAlias,
                      child: ReorderableListView.builder(
                        padding: const EdgeInsets.symmetric(vertical: 8),
                        buildDefaultDragHandles: false,
                        itemCount: widget.config.subdevices.length,
                        onReorder: (oldIndex, newIndex) {
                          setState(() {
                            if (newIndex > oldIndex) newIndex -= 1;
                            final item = widget.config.subdevices.removeAt(oldIndex);
                            widget.config.subdevices.insert(newIndex, item);
                          });
                        },
                        itemBuilder: (context, index) {
                          final sub = widget.config.subdevices[index];
                          return ListTile(
                            key: ObjectKey(sub),
                            leading: ReorderableDragStartListener(index: index, child: const Icon(Icons.drag_indicator)),
                            title: Text(sub.runtimeType.toString()),
                            onTap: () => showDialog(context: context, builder: (_) => sub.configure(context)),
                            trailing: IconButton(icon: const Icon(Icons.delete), onPressed: () { setState(() => widget.config.subdevices.removeAt(index)); }),
                          );
                        },
                      ),
                    ),
                  ),
                ],
              ],
            ),
          ),
        ),
      ],
    );
  }
}
```

From RESEARCH.md §Code Examples (the integration + golden patterns to mirror):
```dart
group('AdvantysSTBStack full-stack integration (QUAL-07)', () {
  testWidgets('1× NIP + 1× PDT + 1× DDI + 1× DDO renders cleanly', (tester) async {
    final stack = AdvantysSTBStackConfig()
      ..subdevices = [
        STBNIP2311Config.preview(),
        STBPDT3100Config.preview(),
        STBDDI3725Config(nameOrId: 'DI', rawStateKey: 'plc.di.raw'),
        STBDDO3705Config(nameOrId: 'DO', rawStateKey: 'plc.do.raw'),
      ]
      ..size = const RelativeSize(width: 0.8, height: 0.3);

    await tester.pumpWidget(
      ProviderScope(
        overrides: [stateManProvider.overrideWith((ref) async => _FakeStateMan())],
        child: MaterialApp(home: Scaffold(body: Center(child: Builder(builder: (ctx) => stack.build(ctx))))),
      ),
    );
    await tester.pump(const Duration(milliseconds: 100));
    expect(tester.takeException(), isNull);
  });
});
```

From test/page_creator/assets/advantys_stb_test.dart (existing infrastructure — reuse, DO NOT duplicate):
- `class _FakeStateMan extends Fake implements StateMan {}` lives at line 2674 — reuse it.
- Imports at lines 1-21 already cover `ProviderScope`, `flutter_test`, all four STB configs, registry, `stateManProvider`.
- The golden harness pattern (`RepaintBoundary` + `Key` + `tester.pump(Duration.zero)` + macOS-gated via `skip: !Platform.isMacOS`) is established at lines 327-501 for DDI3725 — mirror that shape for the stack goldens.

From dart_test.yaml (verified empty-of-tag-relevance):
```yaml
tags:
  golden:
    skip: "Golden tests only run locally with --update-goldens"
```
Phase 1-4 goldens do NOT apply the `golden` tag — they rely solely on `skip: !Platform.isMacOS`. Mirror that. (The `tags:` registration is project-wide but Phase 1-4 chose Platform-gate over tag-gate; do not deviate.)

From lib/page_creator/assets/advantys_stb.dart (Phase 1-4 leaf surface — verified by grep):
- `STBDDI3725Config.build()` returns a `_STBDDI3725` ConsumerStatefulWidget that contains a `GestureDetector(HitTestBehavior.opaque)` wrapping `STBDDI3725Widget`.
- `STBDDO3705Config.build()` likewise wraps in `GestureDetector(opaque)`.
- `STBNIP2311Config.build()` returns `_STBNIP2311` (StatelessWidget) → `STBNIP2311Widget` — NO GestureDetector.
- `STBPDT3100Config.build()` returns `_STBPDT3100` (ConsumerStatefulWidget) — NO GestureDetector (decorative single LED).
(Per RESEARCH finding 4 — the tap-pass-through assertion in QUAL-07 must respect this asymmetry.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the Plan-01 stub configure() with the real _AdvantysSTBStackConfigContent dialog (Size + Coordinates + filtered Add dropdown + ReorderableListView + delete IconButton)</name>
  <files>lib/page_creator/assets/advantys_stb.dart, test/page_creator/assets/advantys_stb_test.dart</files>
  <read_first>
    - lib/page_creator/assets/beckhoff.dart (lines 136-285 — `_CXxxxxConfigContent` source pattern, the LINE-BY-LINE mirror target)
    - lib/page_creator/assets/advantys_stb.dart (the file after Plan 01 — find the placeholder `configure()` and the `_availableSTBSubdevices` Map at the bottom; the dialog State class will be appended directly after the `_STBSubdeviceNormalized` widget)
    - .planning/phases/05-advantysstbstack-composite-parent/05-01-SUMMARY.md (line numbers of the stub `configure()` and the `_STBSubdeviceNormalized` widget — produced by Plan 01)
    - .planning/phases/05-advantysstbstack-composite-parent/05-RESEARCH.md (Pattern 4 — Configure Dialog; Assumption A4 — no delete confirmation; Pitfall 6 — ObjectKey)
    - .planning/phases/05-advantysstbstack-composite-parent/05-CONTEXT.md (Configure Dialog section — note: lines 36-39 say "with confirmation" for delete and imply `nameOrId` on the stack; CX5010 verbatim-mirror commitment overrides both. Checker iteration 1 warnings #2 and #3 surface these to the user post-execution.)
    - test/page_creator/assets/advantys_stb_test.dart (lines 230-280 — the `STBDDI3725Config.configure — editor surface` widget-test pattern to mirror; lines 2670-2829 — append point + `_FakeStateMan`)
  </read_first>
  <behavior>
    Test cases for the configure dialog (append a new test group at the end of test/page_creator/assets/advantys_stb_test.dart, immediately before `_FakeStateMan`):

    Test group `AdvantysSTBStack configure dialog`:
      - Test 1 (add subdevice via dropdown — STACK-04 add gate):
        - Build an empty `AdvantysSTBStackConfig`.
        - Pump it inside `ProviderScope(overrides: [stateManProvider.overrideWith((ref) async => _FakeStateMan())], child: MaterialApp(home: Scaffold(body: SizedBox(width: 800, height: 500, child: cfg.configure(context))))).
        - Find the DropdownButtonFormField. Tap it. Expect exactly 4 DropdownMenuItem widgets (matching `_availableSTBSubdevices.keys`).
        - Select `"STBDDI3725 (16-Ch DI)"` (the displayName-style key from Plan 01). Pump.
        - Assert `cfg.subdevices.length == 1` and `cfg.subdevices[0] is STBDDI3725Config`.
      - Test 2 (dropdown is FILTERED to STB types only — STACK-04 whitelist gate):
        - Tap the dropdown. Capture the menu items.
        - Assert: `expect(items, hasLength(4))` and `expect(itemTexts, containsAll(['STBNIP2311 (Ethernet Head)', 'STBPDT3100 (24 VDC PDM)', 'STBDDI3725 (16-Ch DI)', 'STBDDO3705 (16-Ch DO)']))`.
        - **(Per checker iteration 1 warning #5: do NOT add a negative `itemTexts.any((t) => t.contains('Button') || t.contains('LED') || t.contains('Beckhoff'))` assertion. `hasLength(4)` + `containsAll([...])` already proves the dropdown is bounded to the four whitelist entries; the negative assertion is brittle and redundant.)**
      - Test 3 (reorder via ReorderableListView — STACK-04 reorder gate):
        - Build a stack with 3 subdevices in order [DI, DO, PDT] (use `STBDDI3725Config(nameOrId: 'A')`, `STBDDO3705Config(nameOrId: 'B')`, `STBPDT3100Config(nameOrId: 'C')`).
        - Pump the configure dialog.
        - Find the ReorderableListView. Use `tester.drag` on the first list tile's drag handle (the `Icon(Icons.drag_indicator)` wrapped in `ReorderableDragStartListener`) downward by ~100px to swap items 0 and 1, then pump.
        - Assert the new order is [DO, DI, PDT] (the underlying `cfg.subdevices` list mutated in place).
        - NOTE: ReorderableListView drag-and-drop in widget tests is finicky; if `tester.drag` doesn't trigger the reorder reliably, fall back to invoking the `onReorder` callback directly via a `find.byType(ReorderableListView)` widget reference (acceptable per RESEARCH §Pitfall 6 — testing the ordering invariant matters more than the drag gesture mechanic).
      - Test 4 (remove via delete IconButton — STACK-04 delete gate):
        - **Inline comment in the test body (REQUIRED): `// No confirmation dialog — CX5010 parity per CONTEXT.md line 38 verbatim-mirror commitment, overriding the §Configure Dialog "with confirmation" phrasing. See checker iteration 1 warning #2; SUMMARY must list this as a user-decision item.`**
        - Build a stack with 2 subdevices.
        - Pump the configure dialog.
        - Find the trailing `IconButton(icon: Icon(Icons.delete))` for the first list tile. Tap it. Pump.
        - Assert `cfg.subdevices.length == 1` and the remaining subdevice is the originally-second one.
        - Assert NO `AlertDialog` was shown (no confirmation — CX5010 parity per Assumption A4).
      - Test 5 (no nameOrId field on the stack — CX5010 parity):
        - **Inline comment in the test body (REQUIRED): `// No nameOrId field on the stack — CX5010 parity. The four leaf modules carry their own. Deviates from CONTEXT §Specifics implication; verbatim-mirror commitment overrides. See checker iteration 1 warning #3; SUMMARY must list this as a user-decision item.`**
        - Pump the configure dialog for an empty stack.
        - Use `find.widgetWithText(TextField, 'Name')`, `find.widgetWithText(TextFormField, 'nameOrId')`, etc. — assert NONE of these finders match. The left pane MUST only contain `SizeField` + `CoordinatesField`.
      - Test 6 (CoordinatesField has enableAngle=true — RESEARCH Q3):
        - Pump the configure dialog. Find `CoordinatesField` via `find.byType(CoordinatesField)`.
        - **Assertion (LOCKED per checker iteration 1 warning #4 — use the verbatim widget-access mechanic):**
          ```dart
          expect(tester.widget<CoordinatesField>(find.byType(CoordinatesField)).enableAngle, isTrue);
          ```
          Do NOT use `find.byWidgetPredicate(...)` or assert via the public `widget` getter chained from `tester.firstWidget(...)` — the form above is the project's standard mechanic for reading a widget's named field in a widget test (reuse pattern, no surprises).

    Implementation:
      1. Open lib/page_creator/assets/advantys_stb.dart. Locate the Plan-01 stub `configure()` on `AdvantysSTBStackConfig` — it currently returns `const SizedBox(width: 800, height: 500, child: Center(child: Text('Configure dialog implemented in Plan 02')))`.
      2. Replace it with the CX5010 shape:
         ```
         @override
         Widget configure(BuildContext context) {
           return SizedBox(
             width: 800,
             height: 500,
             child: _AdvantysSTBStackConfigContent(config: this),
           );
         }
         ```
      3. Append, after the existing `_STBSubdeviceNormalized` widget at the bottom of the file, the new `_AdvantysSTBStackConfigContent` StatefulWidget + State class. The code is a VERBATIM clone of `_CXxxxxConfigContent` / `_CXxxxxConfigContentState` from `lib/page_creator/assets/beckhoff.dart:136-285` with these substitutions ONLY:
         - Class name `_CXxxxxConfigContent` → `_AdvantysSTBStackConfigContent`.
         - State class `_CXxxxxConfigContentState` → `_AdvantysSTBStackConfigContentState`.
         - Type `BeckhoffCX5010Config` → `AdvantysSTBStackConfig` (config field type only).
         - Title text `'CX5010'` → `'Advantys STB Stack'` (the `Theme.of(context).textTheme.titleMedium` Text label at the top of the LEFT pane).
         - Map reference `_availableSubdevices` → `_availableSTBSubdevices` (TWO occurrences in the original: the `items:` `.keys.map(...)` and the lookup `_availableSubdevices[v]!`).
         - Everything else (SizedBox.fromSize, Padding, SizeField, CoordinatesField with `enableAngle: true`, the Row("Subdevices") + Spacer + Done FilledButton, the Dropdown, the empty-state Centered Text, the ReorderableListView.builder with `buildDefaultDragHandles: false`, the ListTile with `ObjectKey(sub)` + ReorderableDragStartListener + onTap showDialog → sub.configure + trailing delete IconButton without confirmation): KEEP VERBATIM.
      4. Verify no Riverpod imports needed (the dialog State class is a plain StatefulWidget — `_CXxxxxConfigContent` is also plain `State<...>`, not `ConsumerState`; per RESEARCH §Anti-Patterns "no ref.watch from inside the composite").
      5. Run `flutter test test/page_creator/assets/advantys_stb_test.dart --plain-name "AdvantysSTBStack configure dialog" --reporter compact`. All 6 tests pass.
      6. Run `flutter analyze lib/page_creator/assets/advantys_stb.dart` — zero issues.
  </behavior>
  <action>
    1. Read lib/page_creator/assets/advantys_stb.dart (use the line range from `.planning/phases/05-advantysstbstack-composite-parent/05-01-SUMMARY.md`) and locate the placeholder `configure()` plus the `_STBSubdeviceNormalized` widget appended by Plan 01.
    2. Add the dialog tests (group `AdvantysSTBStack configure dialog`, 6 tests) to test/page_creator/assets/advantys_stb_test.dart immediately before `class _FakeStateMan extends Fake implements StateMan {}` (line ~2670+ in the post-Plan-01 file). Pay special attention to:
       - Test 2: assert with `hasLength(4)` + `containsAll([...])` ONLY. NO negative `.any(...)` assertion (warning #5).
       - Test 4: include the inline comment about CX5010-parity / no-confirmation / CONTEXT line 38 / SUMMARY user-decision item (warning #2).
       - Test 5: include the inline comment about no-nameOrId / CONTEXT §Specifics deviation / SUMMARY user-decision item (warning #3).
       - Test 6: use the LOCKED widget-access mechanic `expect(tester.widget<CoordinatesField>(find.byType(CoordinatesField)).enableAngle, isTrue);` (warning #4).
       Run tests; they MUST fail (the dialog still says "implemented in Plan 02"). Commit: `test(05-02): RED — configure dialog widget tests for AdvantysSTBStack (STACK-04)`.
    3. Replace the placeholder `configure()` method body on `AdvantysSTBStackConfig` with a `SizedBox(width: 800, height: 500, child: _AdvantysSTBStackConfigContent(config: this))`.
    4. Append `_AdvantysSTBStackConfigContent` + `_AdvantysSTBStackConfigContentState` at the bottom of advantys_stb.dart, AFTER `_STBSubdeviceNormalized`. Mirror `lib/page_creator/assets/beckhoff.dart:136-285` line-by-line with the substitutions in the behavior section. Do NOT add any feature beyond CX5010 parity (no confirmation dialog, no nameOrId field, no additional buttons).
    5. Run `flutter test test/page_creator/assets/advantys_stb_test.dart --plain-name "AdvantysSTBStack configure dialog" --reporter compact`. All 6 tests must pass.
    6. Run `flutter analyze lib/page_creator/assets/advantys_stb.dart`. Zero issues. Commit: `feat(05-02): _AdvantysSTBStackConfigContent dialog — filtered Add + reorder + delete (STACK-04)`.

    Anti-patterns to AVOID:
    - DO NOT add a delete confirmation dialog. CX5010 parity per Assumption A4 and CONTEXT amendment in RESEARCH Pattern 4. CONTEXT §Configure Dialog "with confirmation" is overridden by the stronger "Mirrors BeckhoffCX5010Config verbatim" commitment. (Checker iteration 1 warning #2 — SUMMARY must surface this as a user-decision item.)
    - DO NOT add a `nameOrId` field to the stack. The four leaf modules carry their own; the stack is just a container. CX5010 parity per RESEARCH §Open Questions Q2. (Checker iteration 1 warning #3 — SUMMARY must surface this as a user-decision item.)
    - DO NOT use `ConsumerStatefulWidget`/`ConsumerState`. The dialog is plain `StatefulWidget` — the composite does not subscribe to anything (RESEARCH §Anti-Patterns).
    - DO NOT use `ValueKey<String>(...)` on the ListTile. Use `ObjectKey(sub)` exactly as CX5010 (Pitfall 6).
    - DO NOT modify `_availableSTBSubdevices` to include non-STB types or to use class-name keys instead of displayName keys. The map shape was locked in Plan 01.
    - DO NOT add a negative `itemTexts.any((t) => t.contains('Button') ...)` assertion in Test 2 — it is brittle and redundant; `hasLength(4)` + `containsAll([...])` is the locked form (checker iteration 1 warning #5).
    - DO NOT use any other mechanic for the enableAngle assertion in Test 6 except the locked `expect(tester.widget<CoordinatesField>(find.byType(CoordinatesField)).enableAngle, isTrue);` form (checker iteration 1 warning #4).
  </action>
  <verify>
    <automated>flutter test test/page_creator/assets/advantys_stb_test.dart --plain-name "AdvantysSTBStack configure dialog" --reporter compact && flutter analyze lib/page_creator/assets/advantys_stb.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class _AdvantysSTBStackConfigContent" lib/page_creator/assets/advantys_stb.dart` returns `1`
    - `grep -c "_AdvantysSTBStackConfigContent(config: this)" lib/page_creator/assets/advantys_stb.dart` returns `1`
    - `grep -c "_availableSTBSubdevices" lib/page_creator/assets/advantys_stb.dart` is `>= 3` (Plan 01 declaration + 2 dialog use sites)
    - `grep -c "ReorderableListView.builder" lib/page_creator/assets/advantys_stb.dart` returns `1`
    - `grep -c "Icons.delete" lib/page_creator/assets/advantys_stb.dart` returns `1`
    - `grep -c "enableAngle: true" lib/page_creator/assets/advantys_stb.dart` returns `1`
    - `grep -v '^[[:space:]]*//' lib/page_creator/assets/advantys_stb.dart | grep -c "AlertDialog"` returns `0` (no delete confirmation)
    - `grep -v '^[[:space:]]*//' lib/page_creator/assets/advantys_stb.dart | grep -c "implemented in Plan 02"` returns `0` (stub removed)
    - `grep -c "AdvantysSTBStack configure dialog" test/page_creator/assets/advantys_stb_test.dart` returns `1`
    - `grep -c "tester.widget<CoordinatesField>(find.byType(CoordinatesField)).enableAngle" test/page_creator/assets/advantys_stb_test.dart` returns `1` (warning #4 — locked widget-access mechanic)
    - `grep -c "CONTEXT.md line 38" test/page_creator/assets/advantys_stb_test.dart` returns `>= 1` (warning #2 — inline comment in Test 4 referencing CX5010-parity / no-confirmation deviation)
    - `grep -c "CONTEXT §Specifics" test/page_creator/assets/advantys_stb_test.dart` returns `>= 1` (warning #3 — inline comment in Test 5 referencing no-nameOrId deviation)
    - `grep -c "itemTexts.any" test/page_creator/assets/advantys_stb_test.dart | grep -v '^[[:space:]]*//'` returns `0` in the new `AdvantysSTBStack configure dialog` group (warning #5 — negative assertion dropped)
    - `flutter test test/page_creator/assets/advantys_stb_test.dart --plain-name "AdvantysSTBStack configure dialog" --reporter compact` exits 0 with all 6 new tests passing.
    - `flutter analyze lib/page_creator/assets/advantys_stb.dart` exits 0 with zero issues.
  </acceptance_criteria>
  <done>
    The stack's configure dialog matches the CX5010 shape line-by-line with the locked STB whitelist. Add / reorder / delete flows are covered by widget tests. The stub from Plan 01 is gone. Test 6 uses the locked `tester.widget<CoordinatesField>(find.byType(...)).enableAngle` mechanic (warning #4). Tests 4 and 5 carry inline comments calling out the CX5010-parity deviations from CONTEXT (warnings #2 and #3) and require SUMMARY callouts. Test 2 uses `hasLength(4)` + `containsAll([...])` only (warning #5).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add QUAL-07 full-stack integration test + the two `stack_full_{light,dark}.png` goldens; final QUAL-06 analyze gate</name>
  <files>lib/page_creator/assets/advantys_stb.dart, test/page_creator/assets/advantys_stb_test.dart, test/page_creator/assets/goldens/advantys_stb/stack_full_light.png, test/page_creator/assets/goldens/advantys_stb/stack_full_dark.png</files>
  <read_first>
    - test/page_creator/assets/advantys_stb_test.dart (lines 281-306 — `STBDDI3725Widget — mount sanity` pattern; lines 327-501 — DDI3725 golden harness; lines 519-602 — DDI3725 detail dialog trigger pattern for the GestureDetector tap test)
    - lib/page_creator/assets/advantys_stb.dart (post-Task-1 file — verify `_STBDDI3725` and `_STBDDO3705` build paths still wrap in `GestureDetector(HitTestBehavior.opaque)` — this is the substrate for the tap-routing assertion)
    - lib/page_creator/assets/beckhoff.dart (lines 30-110 — confirms CX5010 has no `GestureDetector` wrapping its subdevices either — the per-subdevice GestureDetector lives in each leaf widget, not in the composite. This is the asymmetry RESEARCH finding 4 covers.)
    - .planning/phases/05-advantysstbstack-composite-parent/05-RESEARCH.md (§Code Examples — integration test + golden test snippets; §Pitfall 5 — tap-pass-through correctness; §Validation Architecture — Phase Requirements → Test Map)
    - dart_test.yaml (golden tag config — verify Phase 1-4 used `skip: !Platform.isMacOS` only, NOT `tags: ['golden']`)
  </read_first>
  <behavior>
    Test groups to append to test/page_creator/assets/advantys_stb_test.dart (immediately before `_FakeStateMan` definition, AFTER the Task-1 configure-dialog group):

    Test group `AdvantysSTBStack full-stack integration (QUAL-07)`:
      - Test 1 (renders cleanly — RESEARCH §Code Examples mount-sanity pattern):
        - Build a stack with `subdevices = [STBNIP2311Config.preview(), STBPDT3100Config.preview(), STBDDI3725Config(nameOrId: 'DI', rawStateKey: 'plc.di.raw'), STBDDO3705Config(nameOrId: 'DO', rawStateKey: 'plc.do.raw')]` and `size = const RelativeSize(width: 0.8, height: 0.3)`.
        - Pump inside `ProviderScope(overrides: [stateManProvider.overrideWith((ref) async => _FakeStateMan())], child: MaterialApp(home: Scaffold(body: Center(child: Builder(builder: (ctx) => stack.build(ctx))))))`.
        - `await tester.pump(const Duration(milliseconds: 100))`.
        - Assert `tester.takeException()` is `null`.
        - Assert `find.byType(STBNIP2311Widget)` finds 1, `find.byType(STBPDT3100Widget)` finds 1 (or whatever the leaf widget class names are in advantys_stb.dart — verify by grep), `find.byType(STBDDI3725Widget)` finds 1, `find.byType(STBDDO3705Widget)` finds 1.
      - Test 2 (stack.allKeys returns the de-duplicated union — non-widget unit, mirrors RESEARCH §Code Examples second snippet; matches lines added to all_keys_test.dart in Plan 01 but tests with all four leaves in one place):
        - Build a stack with `subdevices = [STBDDI3725Config(rawStateKey: 'di.raw', forceValuesKey: 'di.force'), STBDDO3705Config(rawStateKey: 'do.raw'), STBPDT3100Config(inputOkKey: 'pdt.ok'), STBNIP2311Config()]`.
        - Assert `stack.allKeys` containsAll `['di.raw', 'di.force', 'do.raw', 'pdt.ok']`.
        - Assert `stack.allKeys` has length 4 (NIP contributes nothing — decorative; no duplicates).
      - Test 3 (tap on DDI body fires its GestureDetector — RESEARCH §Pitfall 5):
        - Pump the canonical 4-module stack inside ProviderScope + MaterialApp/Scaffold.
        - Find the `STBDDI3725Widget`. Tap it. Pump.
        - Expect an `AlertDialog` to appear (DDI's tap-to-open detail dialog — established in Phase 1).
        - Close the dialog (`Navigator.of(context).pop()` or tap outside) to leave a clean tester state.
      - Test 4 (tap on DDO body fires its GestureDetector — RESEARCH §Pitfall 5):
        - Same pattern as Test 3 with `STBDDO3705Widget`. Expect an `AlertDialog` (DDO's detail dialog from Phase 2).
      - Test 5 (tap on NIP body does NOT throw and does NOT open a dialog — RESEARCH §Pitfall 5):
        - Pump the canonical 4-module stack.
        - Find the `STBNIP2311Widget`. Tap it. Pump.
        - Assert `find.byType(AlertDialog)` finds NOTHING (decorative — no tap handler).
        - Assert `tester.takeException()` is `null`.
      - Test 6 (tap on PDT body does NOT throw and does NOT open a dialog — RESEARCH §Pitfall 5):
        - Same pattern as Test 5 with `STBPDT3100Widget`.

    Test group `AdvantysSTBStack goldens` (macOS-gated via `skip: !Platform.isMacOS ? 'Golden tests only run on macOS' : null` — exact mirror of the existing `STBDDI3725 goldens` group at line 327):
      - Constants: `const goldenKey = Key('stb_stack_full_golden');`
      - Helper `Future<void> pumpStack(WidgetTester tester, {required Brightness theme})`:
        - Build the canonical stack: `subdevices = [STBNIP2311Config.preview(), STBPDT3100Config.preview(), STBDDI3725Config.preview(), STBDDO3705Config.preview()]`.
        - Pump inside `ProviderScope(overrides: [stateManProvider.overrideWith((ref) async => _FakeStateMan())], child: MaterialApp(theme: theme == Brightness.dark ? ThemeData.dark() : ThemeData.light(), home: Scaffold(body: Center(child: RepaintBoundary(key: goldenKey, child: SizedBox(width: 800, height: 200, child: Builder(builder: (ctx) => stack.build(ctx))))))))`.
        - `await tester.pump(const Duration(milliseconds: 100))` (NEVER `pumpAndSettle()` — Pitfall 6 from Phase 1 goldens).
      - Test `stack_full_light.png`: `await pumpStack(tester, theme: Brightness.light); await expectLater(find.byKey(goldenKey), matchesGoldenFile('goldens/advantys_stb/stack_full_light.png'));`
      - Test `stack_full_dark.png`: same with `Brightness.dark` and `'goldens/advantys_stb/stack_full_dark.png'`.

    On macOS dev hardware:
      - Run `flutter test test/page_creator/assets/advantys_stb_test.dart --plain-name "AdvantysSTBStack goldens" --update-goldens` to generate the two PNGs.
      - Visually inspect both PNGs at `test/page_creator/assets/goldens/advantys_stb/`. Confirm: NIP on the LEFT, PDT next, DDI next, DDO on the RIGHT — all height-normalized to the same row height; light/dark differ only in the Scaffold background (cream module bodies stay invariant per QUAL-02).
      - Re-run without `--update-goldens` and confirm both goldens pass.

    On non-macOS hardware: the golden group is skipped (per `skip:` clause) — the executor must NOT attempt to generate goldens on Linux; flag it as a manual step in the SUMMARY and move on.

    Final phase-wide gates:
      - `flutter test test/page_creator/assets/advantys_stb_test.dart --reporter compact` — every test (Phase 1-4 + new Phase 5) passes (excluding goldens on non-macOS, which skip cleanly).
      - `flutter test test/page_creator/` — covers `all_keys_test.dart` + cross-file regressions.
      - `flutter analyze` (whole project) — zero issues (QUAL-06 final gate).
  </behavior>
  <action>
    1. Verify Phase 1-4 leaf widget class names (`STBDDI3725Widget`, `STBDDO3705Widget`, `STBNIP2311Widget`, `STBPDT3100Widget`) exist at the expected paths by grep:
       ```
       grep -n "class STBDDI3725Widget\|class STBDDO3705Widget\|class STBNIP2311Widget\|class STBPDT3100Widget" lib/painter/advantys_stb/*.dart
       ```
       If any class name differs, USE the actual class name in `find.byType(...)` for the integration tests. Record the verified class names in the SUMMARY.
    2. Append the `AdvantysSTBStack full-stack integration (QUAL-07)` test group (6 tests) to `test/page_creator/assets/advantys_stb_test.dart` immediately before `class _FakeStateMan` (post-Task-1 line, which `05-01-SUMMARY.md` should record).
    3. Append the `AdvantysSTBStack goldens` test group (2 tests, macOS-gated) immediately after the integration group.
    4. Run `flutter test test/page_creator/assets/advantys_stb_test.dart --plain-name "AdvantysSTBStack full-stack integration" --reporter compact`. All 6 tests must pass.
    5. On macOS: run `flutter test test/page_creator/assets/advantys_stb_test.dart --plain-name "AdvantysSTBStack goldens" --update-goldens` to generate the two PNGs at `test/page_creator/assets/goldens/advantys_stb/stack_full_light.png` and `stack_full_dark.png`. Then re-run without `--update-goldens` and confirm they pass. Visually inspect both. On non-macOS: skip step 5; the SUMMARY must flag "goldens require macOS regeneration".
    6. Run `flutter test test/page_creator/ --reporter compact` to catch cross-file regressions.
    7. Run `flutter analyze` (no path args — whole project) and confirm zero issues. This is the final QUAL-06 gate.
    8. Commit: `feat(05-02): full-stack integration test + 2 goldens (QUAL-06, QUAL-07)`.

    Anti-patterns to AVOID:
    - DO NOT assert "tapping NIP opens a dialog" (RESEARCH Pitfall 5 — NIP has no GestureDetector). Asserting no-exception-and-no-dialog is the correct test.
    - DO NOT assert "tapping PDT opens a dialog" (same — PDT decorative only).
    - DO NOT call `pumpAndSettle()` in the golden harness (Phase-1 pitfall — non-deterministic animation frame; use `tester.pump(Duration(milliseconds: 100))` with explicit duration).
    - DO NOT add the `golden` tag to the goldens group. Phase 1-4 used `skip: !Platform.isMacOS` only (verified in dart_test.yaml interaction); mirror that.
    - DO NOT call `flutter analyze` with a narrow path. The final QUAL-06 gate is the WHOLE project's analyze; a clean narrow run does not satisfy QUAL-06.
    - DO NOT commit PNGs generated on a non-macOS machine. If the executor is on Linux, leave the goldens as "missing — regenerate on macOS" in the SUMMARY; do NOT submit Linux-rendered PNGs (font fallback differs).
  </action>
  <verify>
    <automated>flutter test test/page_creator/assets/advantys_stb_test.dart --plain-name "AdvantysSTBStack" --reporter compact && flutter test test/page_creator/ --reporter compact && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AdvantysSTBStack full-stack integration (QUAL-07)" test/page_creator/assets/advantys_stb_test.dart` returns `1`
    - `grep -c "AdvantysSTBStack goldens" test/page_creator/assets/advantys_stb_test.dart` returns `1`
    - `grep -c "stb_stack_full_golden" test/page_creator/assets/advantys_stb_test.dart` returns `>= 1`
    - `grep -c "stack_full_light.png\|stack_full_dark.png" test/page_creator/assets/advantys_stb_test.dart` returns `>= 2`
    - `grep -c "skip: !Platform.isMacOS" test/page_creator/assets/advantys_stb_test.dart | grep -v '^[[:space:]]*//'` is `>= 5` (existing 4 from Phase 1-4 + the new stack-goldens group).
    - `grep -v '^[[:space:]]*//' test/page_creator/assets/advantys_stb_test.dart | grep -c "pumpAndSettle()"` in the new groups returns `0` (use `tester.pump(Duration(milliseconds: 100))` only).
    - On macOS: both `test/page_creator/assets/goldens/advantys_stb/stack_full_light.png` and `stack_full_dark.png` exist and are non-zero size; both goldens pass without `--update-goldens` after generation.
    - `flutter test test/page_creator/assets/advantys_stb_test.dart --plain-name "AdvantysSTBStack full-stack integration" --reporter compact` exits 0 with all 6 integration tests passing.
    - `flutter test test/page_creator/ --reporter compact` exits 0.
    - `flutter analyze` (no path args, whole project) exits 0 with zero issues (QUAL-06 final gate).
  </acceptance_criteria>
  <done>
    QUAL-07 integration test verifies: clean mount, all four leaf widgets present, `stack.allKeys` correctness, taps register on DDI+DDO (with their detail dialogs opening), taps on NIP+PDT decorative-only paths do not throw. Goldens captured for both themes on macOS. `flutter analyze` clean across the whole project. Phase 5 is shippable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Configure dialog "Add" dropdown → `subdevices` list | The dropdown is the operator's only legitimate path to add a subdevice; bounded to 4 STB types. |
| Page-render path → child widget tap routing | DDI/DDO subdevices receive taps via their own `GestureDetector(opaque)`; NIP/PDT decorative paths must not crash on stray taps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | Operator could circumvent the filtered dropdown by hand-editing JSON | mitigate | Already mitigated by Plan 01's post-`fromJson` sanitiser (STACK-05). Restated here as defence-in-depth: the dropdown filter is the FIRST gate; the sanitiser is the SECOND. Both must be present. The dialog tests in this plan verify the dropdown is bounded to 4 entries via `hasLength(4)` + `containsAll([...])` (per warning #5: positive assertions only — no brittle negative `.any(...)`). |
| T-05-07 | Repudiation | Operator deletes a subdevice by accident; no audit trail | accept | CX5010 parity — no confirmation dialog, no undo. Acceptable for an HMI config dialog with `TFC_GOD` gating already in place at the page-editor level. Deviates from CONTEXT §Configure Dialog "with confirmation" phrasing; verbatim-mirror commitment overrides (warning #2; SUMMARY user-decision item). |
| T-05-08 | Denial of service | Integration test holds a real `StateMan` subscription | mitigate | `stateManProvider.overrideWith((ref) async => _FakeStateMan())` — the empty `Fake implements StateMan` substitutes for the real OPC UA stack; no network, no subscription leaks. |
| T-05-09 | Information disclosure | Dialog displays raw class names (`runtimeType.toString()`) in the ListTile title — could leak internal class structure to a casual screenshot | accept | CX5010 parity. The class names are already in the saved-page JSON (visible via DevTools) — no incremental disclosure. |
</threat_model>

<verification>
**Required commands (all must pass):**
1. `flutter test test/page_creator/assets/advantys_stb_test.dart --plain-name "AdvantysSTBStack configure dialog" --reporter compact` — all 6 dialog tests pass.
2. `flutter test test/page_creator/assets/advantys_stb_test.dart --plain-name "AdvantysSTBStack full-stack integration" --reporter compact` — all 6 integration tests pass.
3. (On macOS only) `flutter test test/page_creator/assets/advantys_stb_test.dart --plain-name "AdvantysSTBStack goldens" --reporter compact` — both goldens pass.
4. `flutter test test/page_creator/ --reporter compact` — no cross-file regression (Phase 1-4 suite still green).
5. `flutter analyze` (whole project, no path args) — zero issues. **This is the QUAL-06 final gate.**

**Manual checks:**
- On macOS dev hardware: visually inspect both PNGs to confirm the NIP+PDT+DDI+DDO layout looks like a real Advantys STB stack (cream bodies, dual RJ45 on NIP, single LED on PDT, 16-LED grids on DDI/DDO).
- Confirm the goldens directory now contains `stack_full_light.png` and `stack_full_dark.png` alongside the 20 existing Phase 1-4 PNGs.
</verification>

<success_criteria>
- `_AdvantysSTBStackConfigContent` dialog exists in `lib/page_creator/assets/advantys_stb.dart` as a verbatim mirror of `_CXxxxxConfigContent` (substitutions limited to: class name, config type, title text, Map reference).
- All 6 configure-dialog widget tests pass: add via filtered dropdown, dropdown is restricted to 4 STB types (positive assertions only per warning #5), reorder via ReorderableListView, remove via delete IconButton (no confirmation per warning #2 — inline comment + SUMMARY callout), no nameOrId field (per warning #3 — inline comment + SUMMARY callout), CoordinatesField has enableAngle=true (verified via locked `tester.widget<CoordinatesField>(...).enableAngle` mechanic per warning #4).
- All 6 QUAL-07 integration tests pass: clean mount with 4 module types, allKeys union is correct, taps on DDI/DDO open their detail dialogs, taps on NIP/PDT decorative paths do not throw or open dialogs.
- (macOS) `stack_full_light.png` and `stack_full_dark.png` exist at `test/page_creator/assets/goldens/advantys_stb/` and pass.
- `flutter analyze` (whole project) reports zero issues — QUAL-06 final gate satisfied.
- Phase 1-4 test suite remains green (zero regressions).
- ROADMAP.md Phase 5 success criteria 1, 2, 4, 5 are now demonstrably true (criterion 3 — `allKeys` flat-map — was satisfied in Plan 01 and re-verified here).
</success_criteria>

<output>
After completion, create `.planning/phases/05-advantysstbstack-composite-parent/05-02-SUMMARY.md` per `templates/summary.md`. The summary MUST record:
- The exact lines in `lib/page_creator/assets/advantys_stb.dart` where `_AdvantysSTBStackConfigContent` was appended.
- The count of new tests added to the configure-dialog and integration groups.
- The verified leaf widget class names used in `find.byType(...)` calls (from the grep verification step).
- Whether goldens were generated on macOS or flagged as "regenerate on macOS" if executed on Linux.
- The `flutter analyze` whole-project exit status snapshot (QUAL-06 final gate).
- **EXPLICIT USER-DECISION CALLOUTS (per checker iteration 1 warnings #2 and #3 — REQUIRED for the user to confirm post-execution):**
  - **Decision 1 (no delete confirmation):** "The configure dialog deletes subdevices with NO confirmation step, mirroring `BeckhoffCX5010Config` verbatim. This deviates from CONTEXT.md §Configure Dialog line 38 ('with confirmation'). The 'Mirrors `BeckhoffCX5010Config` verbatim' commitment in CONTEXT §Compose Pattern was treated as the stronger commitment. **Please confirm this matches your intent; if not, file a follow-up plan to add a confirmation dialog.**"
  - **Decision 2 (no `nameOrId` on the stack):** "The stack itself has NO `nameOrId` field — the four leaf modules carry their own. This mirrors CX5010 verbatim and deviates from CONTEXT.md §Specifics implication that stack-level metadata includes `nameOrId`. **Please confirm; if not, file a follow-up plan to add the field and its codegen entry.**"
- The Phase 5 close-out checklist:
  - [ ] STACK-01 (registry — both maps) — Plan 01
  - [ ] STACK-02 (subdevices: List<Asset> + @AssetListConverter) — Plan 01
  - [ ] STACK-03 (allKeys flat-map) — Plan 01
  - [ ] STACK-04 (configure dialog: filtered add + reorder + delete) — Plan 02
  - [ ] STACK-05 (post-fromJson sanitiser) — Plan 01
  - [ ] QUAL-06 (flutter analyze clean across new files) — Plan 02 final
  - [ ] QUAL-07 (full-stack integration test) — Plan 02
- Both user-decision callouts above MUST be answered in the SUMMARY commit message follow-up before Phase 5 is marked closed.
</output>
</output>
