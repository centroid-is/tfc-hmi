---
phase: 04-polish-error-ux-and-ci-hardening
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/page_creator/assets/elevator.dart
  - lib/page_creator/assets/elevator_painter.dart
  - test/page_creator/assets/elevator_widget_test.dart
  - test/page_creator/assets/elevator_painter_test.dart
  - test/page_creator/assets/goldens/elevator/position_50_out_of_range.png
autonomous: true
requirements:
  - ELEV-15
  - QUAL-06
  - QUAL-07
  - QUAL-08
tags:
  - elevator
  - polish
  - error-ux
  - ci-hardening
  - tdd

must_haves:
  truths:
    - "Position values > 100 or < 0 clamp to [0, 100] AND set _isOutOfRange = true"
    - "ElevatorPainter draws an amber outline (Color(0xFFFFA500), 2px stroke) when isOutOfRange = true"
    - "Stale state and out-of-range state are distinct (mutually exclusive in painter rendering)"
    - "Two Elevator widgets on the same page with different positionKeys operate with fully independent _progress notifiers and _hoistedKey state"
    - "Mounting then unmounting Sensor and Elevator widgets does not leak AnimationControllers or stream subscriptions"
  artifacts:
    "lib/page_creator/assets/elevator.dart":
      contains_also:
        - "bool get _isOutOfRange"
        - "isOutOfRange:"
    "lib/page_creator/assets/elevator_painter.dart":
      contains_also:
        - "final bool isOutOfRange"
        - "Color(0xFFFFA500)"

success_criteria:
  - "All Phase 1+2+3 tests still pass (regression-clean)"
  - "ELEV-15 widget tests pass: progress=150 clamped to 1.0 + isOutOfRange=true; progress=-50 clamped to 0.0 + isOutOfRange=true"
  - "QUAL-06 multi-elevator widget test passes 5/5 runs"
  - "QUAL-07 leak test passes (or documents why leak_tracker_flutter_testing was not added — fall back to manual dispose verification via grep)"
  - "Out-of-range golden committed and visually validates"
  - "TDD cadence: tests committed before implementation"
  - "flutter analyze clean"

threat_model:
  - id: T-04-01
    category: DoS
    component: ElevatorPainter
    disposition: mitigate
    mitigation: "isOutOfRange is a simple bool — paint() does a constant-time outline draw, no extra allocations"
  - id: T-04-02
    category: Information Disclosure
    component: out-of-range visual
    disposition: accept
    mitigation: "Outline only signals abnormal state; no PLC values exposed beyond what the position bar already shows"
---

# Plan 04-01: Polish, Error UX & CI Hardening

## Objective

Close the milestone with three production-quality guards: out-of-range fault outline (ELEV-15), multi-elevator independence smoke test (QUAL-06), and a leak test for AnimationController + stream disposal (QUAL-07). Single plan because all three changes are small and operate on the same files.

## Tasks

### Task 1 (RED): Add failing tests for ELEV-15, QUAL-06, QUAL-07

<read_first>
- lib/page_creator/assets/elevator.dart
- lib/page_creator/assets/elevator_painter.dart
- test/page_creator/assets/elevator_widget_test.dart
- test/page_creator/assets/elevator_painter_test.dart
- pubspec.yaml
</read_first>

<action>
1. Append three test groups to `test/page_creator/assets/elevator_widget_test.dart`:

   **Group: "Out-of-range (ELEV-15)"**
   - Test "stream value 150 clamps to progress 1.0 AND sets isOutOfRange=true": pump Elevator with stateMan stub returning 150; pump until tween settles; find the CustomPaint and assert painter.isOutOfRange == true AND painter.progress == 1.0.
   - Test "stream value -50 clamps to progress 0.0 AND sets isOutOfRange=true": same but with -50.
   - Test "stream value 50 yields progress 0.5 AND isOutOfRange=false": same but with 50.

   **Group: "Multi-elevator independence (QUAL-06)"**
   - Test "two elevators with different positionKeys operate independently": pump a Column with Elevator(positionKey='a') and Elevator(positionKey='b'). Drive 'a' to 25%, 'b' to 75%. Assert each elevator's CustomPaint painter has the correct progress (0.25 and 0.75 respectively).

   **Group: "Leak test (QUAL-07)"**
   - Test "mount/unmount Elevator does not leak resources": testWidgets with `experimentalLeakTesting`. Mount Elevator + Sensor child, pump, then pumpWidget(Container()) to unmount, then await tester.pumpAndSettle. Assert no diagnostic warnings about disposed AnimationControllers or active StreamSubscriptions. If `package:leak_tracker_flutter_testing` is not available, fall back to a source-level grep guard test that asserts `dispose()` calls `_animController.dispose()` and stream-cancel logic exists in the elevator state class.

2. Append one golden test to `test/page_creator/assets/elevator_painter_test.dart`:

   **Group: "Out-of-range golden (ELEV-15)"**
   - Test "position_50_out_of_range golden matches": create ElevatorPainter(progress: 0.5, isStale: false, isOutOfRange: true), capture golden as `goldens/elevator/position_50_out_of_range.png`.

3. Run `flutter test test/page_creator/assets/elevator_widget_test.dart` → confirm RED (new tests fail because painter does not yet have isOutOfRange field).

4. Commit: `test(04-01): add failing tests for out-of-range outline, multi-elevator independence, leak test (ELEV-15, QUAL-06, QUAL-07)`
</action>

<acceptance_criteria>
- Test file additions appear in elevator_widget_test.dart and elevator_painter_test.dart (grep `find . -name "elevator_widget_test.dart" -exec grep -c "Out-of-range\|Multi-elevator\|Leak test" {} \;` returns ≥ 3)
- `flutter test test/page_creator/assets/elevator_widget_test.dart` → at least 3 new failures
- TDD commit: `git log --oneline -1` matches `^test\(04-01\):` pattern
</acceptance_criteria>

### Task 2 (GREEN): Implement isOutOfRange logic + outline + multi-elevator independence

<read_first>
- lib/page_creator/assets/elevator.dart (current state)
- lib/page_creator/assets/elevator_painter.dart (current state)
</read_first>

<action>
1. In `lib/page_creator/assets/elevator_painter.dart`:
   - Add field: `final bool isOutOfRange;` to `ElevatorPainter` constructor (default `false`)
   - In `paint()`, after rails+platform are drawn, if `isOutOfRange` is true, draw a 2px amber outline: `canvas.drawRect(Offset.zero & size, Paint()..color = const Color(0xFFFFA500)..strokeWidth = 2..style = PaintingStyle.stroke)`
   - Update `shouldRepaint` to include `oldDelegate.isOutOfRange != isOutOfRange`

2. In `lib/page_creator/assets/elevator.dart` `_ElevatorState`:
   - Add a `bool _isOutOfRange = false` field
   - In the stream listener that updates `_progress`, before clamping, check if value is `< 0` or `> 100`. If so set `_isOutOfRange = true`, else `false`. Then clamp to [0, 100] as before.
   - Pass `isOutOfRange: _isOutOfRange` to `ElevatorPainter` in `build()`.
   - Wrap the `_isOutOfRange` field in `setState()` for rebuild.

3. Multi-elevator independence: should already work since each Elevator instance has its own `_progress` notifier and `_hoistedKey`. The Task 1 test should now pass without code changes — if it fails, investigate shared static state and fix.

4. Run `flutter test test/page_creator/assets/elevator_widget_test.dart` → confirm GREEN (out-of-range and multi-elevator tests pass).

5. Run `flutter test --update-goldens test/page_creator/assets/elevator_painter_test.dart` → generates the new out-of-range golden.

6. Commit: `feat(04-01): add out-of-range outline + verify multi-elevator independence (ELEV-15, QUAL-06)`
</action>

<acceptance_criteria>
- `grep -c "isOutOfRange" lib/page_creator/assets/elevator_painter.dart` ≥ 3
- `grep -c "isOutOfRange" lib/page_creator/assets/elevator.dart` ≥ 2
- `grep -c "0xFFFFA500" lib/page_creator/assets/elevator_painter.dart` ≥ 1
- All elevator widget tests pass
- New golden file exists: `test/page_creator/assets/goldens/elevator/position_50_out_of_range.png`
- `flutter analyze` clean
</acceptance_criteria>

### Task 3: Leak test (QUAL-07) — best effort

<read_first>
- pubspec.yaml
- lib/page_creator/assets/elevator.dart (verify dispose method)
- lib/page_creator/assets/sensor.dart (verify dispose method)
</read_first>

<action>
1. Check if `package:leak_tracker_flutter_testing` is available via `flutter pub deps | grep leak_tracker`. If absent, add to dev_dependencies in pubspec.yaml.

2. If the package is available, write a `testWidgets` with `experimentalLeakTesting` mounting Elevator + Sensor child, then unmounting. Assert no leaks.

3. If the package is unavailable or causes test infrastructure issues, fall back to a source-level guard:
   - Append test "dispose() cancels animation controller and stream subscription" that:
     - Reads `lib/page_creator/assets/elevator.dart` source
     - Asserts the source contains `_animController.dispose()` (or equivalent) inside a `dispose()` method
     - Asserts the source contains `cancel()` or stream cleanup logic

4. Run `flutter test test/page_creator/assets/elevator_widget_test.dart` → confirm leak test passes.

5. Commit: `test(04-01): add leak test for AnimationController + stream disposal (QUAL-07)`
</action>

<acceptance_criteria>
- Either pubspec has leak_tracker_flutter_testing OR a source-level dispose-guard test exists
- Leak test passes
- TDD commit on log
</acceptance_criteria>

### Task 4: Write SUMMARY.md and close milestone

<read_first>
- All Phase 4 changes
- $HOME/.claude/get-shit-done/templates/summary.md
</read_first>

<action>
1. Run full test suite over Phase 1-4 surface (5 deterministic runs):
   ```
   flutter test test/page_creator/assets/sensor_config_test.dart \
                test/page_creator/assets/sensor_painter_test.dart \
                test/page_creator/assets/sensor_widget_test.dart \
                test/page_creator/assets/elevator_layout_test.dart \
                test/page_creator/assets/elevator_config_test.dart \
                test/page_creator/assets/elevator_painter_test.dart \
                test/page_creator/assets/elevator_widget_test.dart
   ```

2. Write `.planning/phases/04-polish-error-ux-and-ci-hardening/04-01-SUMMARY.md` covering:
   - All 3 requirements (ELEV-15, QUAL-06, QUAL-07) — what was built, where verified
   - QUAL-08 (TDD discipline) — count of test/feat commits
   - Phase 4 closeout: milestone complete

3. Commit: `docs(04-01): close Phase 4 — Polish, Error UX & CI Hardening`
</action>

<acceptance_criteria>
- `.planning/phases/04-polish-error-ux-and-ci-hardening/04-01-SUMMARY.md` exists
- Full test suite passes 5/5 runs
- `flutter analyze` clean across all modified files
</acceptance_criteria>
