/// Web stub for ip_settings.dart.
/// Loaded via conditional import on web where nm/dbus are unavailable.
library;
import 'package:flutter/material.dart';

class IpSettingsPage extends StatelessWidget {
  final dynamic dbusClient;

  const IpSettingsPage({super.key, this.dbusClient});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Not available on web'));
  }
}
