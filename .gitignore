# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
# Libraries should not include pubspec.lock, per https://dart.dev/guides/libraries/private-files#pubspeclock.
/pubspec.lock
packages/*/pubspec.lock
**/doc/api/
.dart_tool/
build/
.direnv
.planning/
.flutter-plugins-dependencies
**/test/**/failures/

# ST compiler oracle venv
tools/st-oracle-venv/

# Local data / secrets
timescale_data/
timescale_certs/
.claude/
.mcp.json
page-editor.json

# CentroidX sideload certificate — private key, never commit
centroidx-sideload.pfx

# Local marionette fork (use upstream pub.dev package)
packages/marionette_flutter/

# JSON test reports (`dart/flutter test --file-reporter`), written by the
# jbtm-test, centroid-hmi-test and relay-packages-test CI jobs so they can
# assert the suite actually ran. Ephemeral on the runner; only shows up
# locally if you run the same command by hand.
test-report.json
soak-report.json

# Binary artifacts
*.zip
ATV320_Installation_manual_EN_NVE41289_09.pdf
